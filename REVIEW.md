# REVIEW.md - plan poprawy jakosci (przeglad 2026-07-03)

> **Transient plan doc** (por. CLAUDE.md - "Plan docs have a lifecycle"): usunac po zamknieciu wszystkich etapow.
>
> Zrodlo: wieloagentowy przeglad kodu 2026-07-03 (14 finderow + adwersaryjna weryfikacja kazdego znaleziska;
> 112 surowych -> 96 po dedupie -> 93 potwierdzone, 6 obalonych. Oba HIGH zweryfikowane podwojnie, niezaleznie).

## Jak pracowac z tym planem (sesje niezalezne)

- Jedna sesja = jeden etap (albo domkniecie rozgrzebanego). Etapy sa od siebie NIEZALEZNE poza jednym wyjatkiem:
  Etap 6 najlepiej robic PO Etapie 5 (usuniecie martwego kodu zmniejsza liczbe kopii do konsolidacji).
- Przed praca: przeczytaj sekcje etapu, potem cytowane pliki (Read z offset/limit - pliki sa duze).
- Kazda pozycja byla zweryfikowana adwersaryjnie na stanie repo z 2026-07-03 (commit bf79695). Jesli kod sie
  od tego czasu zmienil, zweryfikuj ponownie przed naprawa; "Problem" opisuje mechanizm, wiec latwo to sprawdzic.
- Po etapie: `npm run build` + `npm run lint` + `npm run typecheck` (wszystko zielone), commit wg sugestii etapu,
  odhacz `[x]` pozycje tutaj i dopisz krotka notke, jesli fix odbiegl od sugerowanego kierunku.
- **NIE piszemy testow w trakcie** - rekoncyliacja testow nastepuje raz, przy release (por. CLAUDE.md).
- Chronione: komentarze game-knowledge, literowki gry (`hightlightPlanet`, `planet-koords`), `#ago_routine_7`,
  plain-moduly w state/, afordancje `_reset*ForTest` / `dispose*Store`.

## Status etapow

- [x] **Etap 1** - Sync i integralnosc danych (9 pozycji)
- [x] **Etap 2** - Psucie danych kolonizacji i skanow (4 pozycji)
- [~] **Etap 3** - Bugi feature-ow (UI/zachowanie) (7/9 zrobione; 3.5+3.9 odlozone - apiContext/index.js w trakcie edycji uzytkownika)
- [x] **Etap 4** - Lifecycle, teardown i architektura (medium) (6 pozycji)
- [x] **Etap 5** - Martwy kod - usuwanie (15 pozycji)
- [ ] **Etap 6** - Duplikacja i centralizacja gameDom (16 pozycji)
- [ ] **Etap 7** - Drobne bugi, race-y i teardown (low) (26 pozycji)
- [ ] **Etap 8** - Dokumentacja i komentarze (6 pozycji)
- [ ] Sprzatanie: usun ten plik po zamknieciu calosci

## Etap 1: Sync i integralnosc danych

Najwyzszy priorytet: oba bugi HIGH + sciezki, gdzie awaria sieci/storage prowadzi do utraty lub wskrzeszenia danych. Pliki: sync/*, state/sharedSettings, lib/storage, features/alarmClock/producer, features/dashboard/alarmClock.

Sugerowany commit: `fix(sync): harden gist/ntfy/storage failure paths`

**Notka (2026-07-03, po adwersaryjnej weryfikacji diffa):** 3 poprawki do pierwszej wersji fixow:
(a) 1.2: seed w initSharedSettings NIE materializuje pustych kluczy tokenow (inaczej token-less origin bootujacy pierwszy klobrowalby niezmigrowany token innego originu);
(b) 1.2+1.9: zapis migracyjny w initSharedSettings jest best-effort (try/catch) — rejectujacy chromeStore.set nie moze zerwac hydratacji i subskrypcji onChanged; analogiczny hardening w seed-IIFE schedulera;
(c) 1.6: scheduleUpload tlumi arm podczas adopcji zdalnych danych w upload() (inaczej kazda runda z remote-merge stranduje flage i boot robi zbedny upload).

- [x] **1.1** `src/features/alarmClock/producer.js:455` **[high/bug]** - Guardian long-press dismiss is a silent no-op for any landing older than 2 hours because the dismiss record's expiry is anchored to landedAt, not now.
  - **Problem:** guardianDismiss (producer.js:453-457) writes addGuardianDismiss(universeId, bodyKey, landedAt, landedAt + GUARDIAN_SUPPRESS_TTL_SEC, now) — expiresAt = landedAt + 7200s. The landed-FS set is durable with NO expiry (domain/fleetSave.js:342 'There is deliberately NO expiry' and reconcileLandedFleetSaves:392 'no timer'), so a landing can easily be >2h old when the player long-presses. Then expiresAt <= now, and guardianDismiss.js prune() (line 63, keeps only expiresAt > now) drops the record the moment it is read: guardian.js refresh() (line 359/369) still sees the landing as undismissed and re-shows the button immediately, and producer.doRun's guardianDismissedLandings (line 366) returns {} so sync/alarmClock.js:632-634 never builds the dismissedKey that would drop the landing at the source. The 1.5s deliberate hold (DISMISS_HOLD_MS) does nothing, forever, for old landings. The sibling ack path is correct: guardianAck (producer.js:461) uses now + GUARDIAN_SUPPRESS_TTL_SEC.
  - **Kierunek naprawy:** Anchor the dismiss record's expiry to the tap time, e.g. addGuardianDismiss(universeId, bodyKey, landedAt, now + GUARDIAN_SUPPRESS_TTL_SEC, now) (or Math.max(landedAt, now) + TTL), matching guardianAck; the TTL's documented job (fleetSave.js:326 'must outlive the gap between the user's tap and the sync') requires it to start at the tap.

- [x] **1.2** `src/state/sharedSettings.js:74` **[high/bug]** - A gistToken/alarmClockNtfyToken cleared in the dashboard is silently resurrected by every game origin, so the user cannot remove a stored secret
  - **Problem:** mergeShared treats an explicitly-cleared cloud token as 'unset': `gistToken: typeof o.gistToken === 'string' && o.gistToken ? o.gistToken : local.gistToken` (same for alarmClockNtfyToken, lines 74-79) back-fills from the game origin's local copy whenever the cloud string is empty. The dashboard's writeShared (features/dashboard/settingsControls.js:49-52) writes `{...cur, gistToken: ''}` when the user empties the field, and every game origin has previously mirrored the token into its per-origin localStorage (applyToStore -> settingsStore.set -> the initSettingsStore diff subscriber writes oge_gistToken). On the next game-page load initSharedSettings (line 103-104) sees `JSON.stringify(raw) !== JSON.stringify(merged)` and writes the resurrected token BACK to chrome.storage, undoing the user's clear; the onChanged path (line 113-115) likewise re-applies the old token to settingsStore. The empty-string back-fill was meant only as a one-time migration for a fresh (absent) slot, but it fires on every deliberate clear too — there is no 'migrated' latch and no absent-vs-empty distinction.
  - **Kierunek naprawy:** Distinguish 'key absent from the cloud dict' (migrate up from local) from 'key present but empty' (authoritative clear): e.g. `gistToken: typeof o.gistToken === 'string' ? o.gistToken : local.gistToken`. That keeps the first-run migration working while letting a dashboard clear propagate — and the game side should also clear its localStorage mirror when the cloud value goes empty.

- [x] **1.3** `src/features/dashboard/alarmClock.js:569` **[medium/robustness]** - The gist PATCH fetch in cancelWaveFromDashboard (and cancelAdhocFromDashboard, line 1029) is not wrapped in try/catch — a network failure becomes an unhandled rejection with no user feedback and a permanently disabled cancel button
  - **Problem:** `const res = await fetch('https://api.github.com/gists/...', { method: 'PATCH', ... })` at lines 569-581 (and 1029-1041) only handles the `!res.ok` branch; a rejected fetch (offline, DNS) propagates out of the async function. Callers invoke it fire-and-forget after disabling the button: `btn.setAttribute('disabled',''); void cancelWaveFromDashboard(universeId, w.id);` (lines 733-736). The doc comment for cancelWaveFromDashboard says 'every failure mode is shown in the preview status line and the dashboard re-renders' (lines 511-512) — false for this path. Note the ntfy DELETEs at line 553 may have already succeeded, so the wave silently loses its pings while the gist still lists them and the user sees nothing.
  - **Kierunek naprawy:** Wrap the PATCH in try/catch mirroring the fetchAllAlarmClockStates handling: setPreviewStatus('cancel failed: ' + err.message, 'err') and void refreshPreview() on rejection.

- [x] **1.4** `src/features/dashboard/alarmClock.js:359` **[medium/bug]** - The orphan sweep can cancel LIVE reminders: a universe file that fails JSON.parse (or is truncated — no raw_url resolution) drops all its message ids out of `ours`, and messages posted by another device between our parallel gist/ntfy fetches are treated as orphans when its file's updatedAt is already >120s old
  - **Problem:** fetchAllAlarmClockStates silently skips a corrupt file (lines 244-249 'Skip corrupt file; next write rebuilds it') — and unlike sync/alarmClock.js:333-336 it does NOT resolve GitHub's truncated case via raw_url, so a >1MB file's truncated content prefix also fails JSON.parse and is dropped — so collectOurMessageIds (line 359) never sees that universe's scheduledMessageIds; the sweep (lines 367-387) then DELETEs every one of that universe's still-queued pings as phantom orphans — exactly the failure class the collectOurMessageIds comment warns about (lines 261-264), and the freshness guard computes freshestAge only over PARSED states, so the skipped universe's recent updatedAt cannot protect its pushes. Separately, the freshness guard compares `nowSec - updatedAt` of the FETCHED snapshot (lines 361-366): gist states and the ntfy queue are fetched in parallel (Promise.all, line 349), so a sibling device that posts messages after our gist read but whose previous file write was >120s ago has its fresh messages swept. Also, `freshestAge` stays Infinity when no state carries updatedAt, which fails OPEN into sweeping.
  - **Kierunek naprawy:** Fail closed: skip the sweep entirely if any alarmClock-named gist file failed to parse or is truncated (or if any state lacks updatedAt), and/or exempt ntfy messages whose scheduled time was created within the last ~120s; also fetch raw_url when file.truncated (matching sync/alarmClock.js).

- [x] **1.5** `src/sync/gist.js:547` **[medium/robustness]** - Truncated-gist raw_url fetch has no res.ok check, converting an HTTP failure into 'empty gist' — which the upload path then treats as license to rebuild the payload and wipe every other universe's slots
  - **Problem:** readGistFile: `if (file.truncated && file.raw_url) { const res = await fetch(file.raw_url); return res.text(); }` — a non-ok response (403/500/proxy error page) returns the error body as content (unlike gh() which throws on non-ok); gzipDecode/JSON.parse in fetchGistData then fail and it returns null. upload() (scheduler.js:848, 872) explicitly distinguishes 'fetch THROWS → abort, never rebuild from null' from 'returns null → first-upload path, seed from local' precisely to avoid wiping other universes' slots (`const map = { ...(remoteRec?.[s.payloadKey] || {}) }` with remoteRec=null drops them all — every OTHER universe's slot and any other device's recent writes are PATCHed away). This branch silently routes an availability failure into the destructive null path. Only reachable when the file exceeds GitHub's 1 MB inline limit (file.truncated), which the header admits 'a user with a huge account could conceivably cross' (pre-lean-sync gists reached 2.6MB).
  - **Kierunek naprawy:** In readGistFile, throw on `!res.ok` for the raw_url fetch (mirroring gh()'s error handling) so an availability failure aborts the round via the caller's try/catch instead of masquerading as an empty gist.

- [x] **1.6** `src/sync/scheduler.js:936` **[medium/bug]** - UPLOAD_PENDING_KEY is cleared unconditionally at the end of upload(), erasing a dirty flag armed by a write that landed mid-flight — re-opening the exact reload-vs-debounce loss the flag was built to close
  - **Problem:** upload() reads local per slot (`await s.readLocal()`, line 875) early in the round, then at line 936 runs `safeLS.remove(UPLOAD_PENDING_KEY)` after the PATCH/skip. If a store write (e.g. a colonize `sent` decision) fires between the local read and line 936, its onStoreChange → scheduleUpload() (line 972) sets the flag and re-arms the 15 s debounce — but line 936 then removes the flag while the write's data was NOT in the PATCH. If OGame's post-send reload kills the debounce (the documented case at lines 228-234), the next boot sees no flag (`if (safeLS.get(UPLOAD_PENDING_KEY)) await upload();`, line 1224) and the write is stranded locally until some unrelated future trigger.
  - **Kierunek naprawy:** Capture a generation/counter when upload() snapshots local (or re-check whether scheduleUpload ran since the snapshot) and only remove UPLOAD_PENDING_KEY when nothing re-armed it during the round.

- [x] **1.7** `src/sync/scheduler.js:1141` **[medium/bug]** - Force-sync (Sync-now click / dashboard tombstone) is silently dropped when the shared inFlight lock is held — the comment claims the operations 'serialise via the shared lock' but the guard makes them no-op skip
  - **Problem:** onForceSync runs `await downloadAndMerge(); await upload();` and its comment (line 1148) says 'they serialise via the shared lock'. But both functions early-return via canStartSync when inFlight is set (lines 743-745, 831-833) — nothing queues or retries. A `oge_syncRequestAt` tombstone from the dashboard fires exactly once (onStorageChange, line 1182); if it arrives while the boot download or a debounced upload is mid-flight, the entire forced round-trip is dropped and the dashboard edit's propagation to the gist waits for the next unrelated trigger. Same for the user's explicit 'Sync now': the click resolves having done nothing, with no status feedback.
  - **Kierunek naprawy:** Make forced rounds queue behind the lock (e.g. a pending-force flag checked in the finally of downloadAndMerge/upload, or await a shared in-flight promise) instead of skipping; at minimum fix the 'serialise' comment to say concurrent calls are dropped.

- [x] **1.8** `src/sync/alarmClock.js:335` **[medium/robustness]** - readAlarmClockState's truncated raw_url fetch lacks a res.ok check; a server error returns null and syncAlarmClock rebuilds the alarm state from scratch, losing cross-device bookkeeping
  - **Problem:** alarmClock.js:333-336: `const text = file.truncated && file.raw_url ? await (await fetch(file.raw_url)).text() : file.content;` — a non-ok response's HTML body reaches JSON.parse (line 338), the catch (350-352) returns null, and syncAlarmClock (lines 572-579) then treats prev state as empty (`prevWaves=[]`, `prevNotify={}`, `prevAdhoc=[]`...). Line 786 `if (changed) await writeAlarmClockState(universeId, next)` persists the rebuilt file: wave `cancelled` tombstones, locked `baseAt` anchors, adhoc alarms armed on another device, and all scheduledMessageIds are dropped. The dashboard orphan sweep (features/dashboard/alarmClock.js:354-385) subsequently cancels the still-queued ntfy pushes whose ids are no longer recorded — silent loss of reminders. The same missing-ok pattern is copy-pasted from gist.js readGistFile.
  - **Kierunek naprawy:** Check res.ok on the raw_url fetch and throw on failure so the sync round aborts (the caller already surfaces thrown errors) instead of converting a transient 5xx into a state rebuild; ideally share one truncation-aware gist-file reader (export readGistFile from gist.js) so both callers abort on an availability failure.

- [x] **1.9** `src/lib/storage.js:213` **[medium/robustness]** - chromeStore.set/get/remove never check chrome.runtime.lastError, so a failed storage write resolves as success — defeating the durability-critical flush* paths.
  - **Problem:** lib/storage.js:206-216 (`set`) and the sibling get/remove wrappers resolve unconditionally in the API callback: `api.local.set({...}, () => { resolve(); })` with no `chrome.runtime.lastError` read (same in get at 184-186 and remove at 225-227). A quota failure or invalidated extension context is reported as success. Callers exist specifically for durability before OGame's forced reload — state/colonizeDecisions.js:100 `flushColonizeDecisionsStore` (called at features/abandon/index.js:101, features/colonyRecorder.js:199, features/sendColony/index.js:613) and state/scans.js:250 `flushScansStore` — and would silently lose the decision they were flushing; every persist() write-through caller (state stores, apiCache, decisions) believes the value was durably stored when it was not. Unchecked lastError also produces 'Unchecked runtime.lastError' console noise in Chrome.
  - **Kierunek naprawy:** Read chrome.runtime.lastError inside each callback; at minimum log via lib/logger, ideally resolve a boolean (or reject) so flush callers and sync's setStatus('err', ...) can surface/retry a failed persist.
  - **Notka (2026-07-03):** zrobione wariantem reject: get/getAll degraduja do undefined/{} (odczyt lastError wymagany przez Chrome), set/remove REJECTUJA przy zgloszonej awarii. Bez lib/logger — storage.js -> logger.js -> storage.js byloby cyklem modulow (logger czyta safeLS przy ewaluacji).

## Etap 2: Psucie danych kolonizacji i skanow

Zapisy, ktore zasmiecaja lub kasuja trwale (i synchronizowane) dane: log decyzji kolonizacyjnych, pola lifeform w SystemScan, wyscig z hydratem store.

Sugerowany commit: `fix(colony): stop corrupting persisted scan/decision data`

- [x] **2.1** `src/features/sendColony/index.js:695` **[medium/bug]** - onCheckTargetResult records a terminal 90-day DEC_TAKEN decision for EVERY checkTarget response, including non-colonize ones (expeditions, spy probes, manual sends, mid-typing transients), polluting the persistent+synced decision log and able to falsely block genuinely free slots.
  - **Problem:** recordDecision(galaxy, system, position, ... { s: DEC_TAKEN, ts: decNow }) at lines 694-702 runs for any errorCode != null && != 140035, or any success where orders['7'] !== true — with no gate on mission/ships/courier session. bridges/checkTargetObserver.js dispatches oge:checkTargetResult for every action=checkTarget XHR (line 209, no mission filter; its own comment at lines 119-121 says the XHR 'fires on every keystroke the user makes in the coord input'). domain/colonizeDecisions.js: DEC_TAKEN is TERMINAL (line 55, never regresses) with a 90-day TTL (line 41) and blockingCoords() blocks it (line 166). So every expedition prepare writes a taken decision for g:s:16, every spy probe writes one for the watched planet, and a transient/mistyped coord over an empty slot whose checkTarget errors (e.g. 'target does not exist') gets a 90-day taken block plus the empty→abandoned scansStore downgrade at lines 706-721 — the exact false-skip the reactor's own comment claims it avoids ('leaves unrelated checkTargets (expeditions, manual sends) alone', line 637).
  - **Kierunek naprawy:** Gate the recordDecision/scan-downgrade on colonize relevance: e.g. only act when detail.ships contains SHIP_COLONY (the courier always selects exactly one colony ship, and the bridge already carries the am-field ship map), or when a colonize courier session/armed target matches the coords.

- [x] **2.2** `src/features/sendColony/index.js:773` **[medium/state-hygiene]** - onColonizeSent's scansStore write rebuilds the SystemScan from scratch and silently erases the lifeform-discovery fields (lfScannedAt / lfPositions) owned by sendLifeform.
  - **Problem:** Line 773 writes `[key]: { scannedAt: existing.scannedAt, positions: newPositions }` — dropping any other fields on `existing`. state/scans.js documents SystemScan's optional lfScannedAt/lfPositions (lines 69-79) and its own merge deliberately spreads the existing record first 'so lifeform discovery markers ... survive' (lines 358-362); the sibling reactor onCheckTargetResult in this same file correctly uses `{ ...existing, positions: ... }` (line 716). Sending a colony ship into a system discovered <7 days ago wipes its lfScannedAt, so isSystemLfStale flips true, the Lifeforms button re-proposes an already-covered system (wasted tap → shipsSent:0) and countLfRemaining over-reports.
  - **Kierunek naprawy:** Spread the existing record: `[key]: { ...existing, positions: newPositions }` (scannedAt is already carried by the spread).

- [x] **2.3** `src/features/abandon/index.js:86` **[medium/bug]** - cleanupAbandonedPlanet rebuilds the SystemScan without spreading the existing entry, silently wiping the system's persisted lifeform-discovery markers (lfScannedAt/lfPositions)
  - **Problem:** scansStore.update returns `[key]: { scannedAt: Date.now(), positions: newPositions }` — it spreads existing.positions but not `...existing`, so the optional SystemScan fields lfScannedAt/lfPositions (state/scans.js:69-79, documented as 'preserved across colonization rescans') are dropped from the in-memory entry. The persist path stripToLfMarkers (state/scans.js:145-164) keeps ONLY entries with lf markers, so on the next debounced save the whole system record disappears from disk — the 7-day sendLifeform retention gate for that system is lost, and the Lifeforms button will re-offer/re-fire a discovery for a system explored under 7 days ago after the user abandons a colony there.
  - **Kierunek naprawy:** Rebuild as `{ ...existing, scannedAt: Date.now(), positions: newPositions }` so the optional lf marker fields survive the abandon cleanup.

- [x] **2.4** `src/features/colonyRecorder.js:196` **[medium/state-hygiene]** - The fresh-colony DEC_MINE decision write races the colonizeDecisionsStore hydrate (no hydration gate exists for that store), so the just-recorded decision is silently wiped and the flush transiently truncates the persisted, gist-synced decision log
  - **Problem:** tryCollect gates its historyStore dedup on whenHistoryHydrated() (line 261) but then calls colonizeDecisionsStore.update(...) + flushColonizeDecisionsStore() (lines 196-199) with no equivalent gate — state/colonizeDecisions.js exposes NO hydration promise at all (persist at lines 67-79, no hydratedPromise/onHydrate-resolve). content.js issues initHistoryStore()'s chrome.storage get (line 114) BEFORE initColonizeDecisionsStore()'s (line 142), so history's hydrate .then — and tryCollect inside it — runs before the decisions hydrate lands: the DEC_MINE update is applied to the pre-hydrate empty map, flush persists that one-entry map (momentarily erasing all stored decisions), then persist.js:96-97 store.set(old stored map) clobbers the new decision and the debounced write-through saves the old map back. This is byte-for-byte the failure mode the same file's header documents for history (lines 44-61), and state/targets.js:12-14 explicitly added whenTargetsHydrated to guard its own writers against 'the colonyRecorder race' — the decisions write here is the unguarded straggler.
  - **Kierunek naprawy:** Add a whenColonizeDecisionsHydrated() promise to state/colonizeDecisions.js (same pattern as state/targets.js) and await it before the withDecision update + flush in tryCollect.

## Etap 3: Bugi feature-ow (UI/zachowanie)

Widoczne dla uzytkownika bledy zachowania. Kazda pozycja niezalezna od pozostalych - mozna robic wybiorczo.

Sugerowany commit: `fix(features): assorted user-visible behavior bugs`

**Notka (2026-07-03):** 7/9 zrobione i scommitowane. **3.5 i 3.9 ODLOZONE** — obie dotykaja
`src/features/apiContext/index.js`, ktory uzytkownik ma otwarty z niescommitowanymi zmianami
(praca nad Galaxy Viewer / Spyglass). Dokonczyc, gdy ten plik bedzie czysty (fix i tak jest
trywialny: incremental write / walidacja pustego parse — kierunki nizej sa nadal aktualne).

- [x] **3.1** `src/features/dashboard/colony.js:120` **[medium/bug]** - Restoring a removed position-filter selection leaves select.value = '' which makes renderAll filter out ALL colony entries, so a universe with data shows the 'No colony data recorded yet' empty state
  - **Problem:** populatePositionFilter saves previousValue, rebuilds options, then does `if (previousValue && previousValue !== selectEl.value) selectEl.value = previousValue;` (lines 98-121). Per the HTML spec, assigning a value with no matching option deselects everything and the getter returns '' (the in-code comment claims Chrome falls back to the first option — it does not). index.js getFilter() (line 876) returns '' (only nullish falls back to 'all'), and renderAll (lines 906-908) then runs `history.filter((e) => e.position === parseInt('', 10))` — parseInt('') is NaN, so every entry is dropped and renderColonyChart paints the empty state. Trigger: select 'Position 8' on universe A, then switch the selector to a universe whose history has no position-8 entries.
  - **Kierunek naprawy:** After the restore attempt, check `if (selectEl.value === '') selectEl.value = 'all';` (or compare selectedIndex === -1), or have getFilter() treat '' as 'all'.

- [x] **3.2** `src/features/sendSpy/index.js:249` **[medium/bug]** - Post-dispatch success path paints 'Sent!' and then immediately calls refresh() with busy already false, so the 'Sent!' feedback is synchronously overwritten and the button re-arms during the post-send reload window (no SENT_LOCK equivalent).
  - **Problem:** Lines 238-249: `busy = false; spyReady = false; ... paintZone({ text: 'Sent!', ... }); refresh();`. refresh() (line 180) sees busy=false and spyReady=false and repaints renderSpy(deriveSpy(...)) — the next candidate — in the same turn, so 'Sent!' never displays. The file header claims the feature is 'modelled 1:1 on features/sendColony/index.js', but sendColony deliberately HOLDS the busy lock for SENT_LOCK_MS after a successful dispatch (index.js lines 457-469, added to stop 'weird unlocked states' before the game's ~1 s reload); sendSpy leaves the button unlocked and re-targeted, so a quick second tap can start a new courier select() against a page that is about to unload.
  - **Kierunek naprawy:** Mirror sendColony: keep busy=true after a successful dispatch, drop the immediate refresh(), and release via a cleared-on-dispose safety setTimeout (SENT_LOCK_MS) in case the reload never comes.

- [x] **3.3** `src/features/badges/index.js:741` **[medium/bug]** - guarded() disconnect/re-attach permanently strips the visibility wiring of the badges observer after the first render, so it keeps observing (and re-rendering) while the tab is hidden
  - **Problem:** guarded() calls observer.disconnect() then attachObserver(observer) on every render (lines 740-747). createVisibilityObserver.disconnect() (src/lib/visibilityObserver.js:64-68) does stop() + document.removeEventListener('visibilitychange', onVisibility) + targets.length=0 — and the visibilitychange listener is added only once at creation (line 55). observe() re-adds targets and restarts the MutationObserver, but nothing ever re-adds the visibilitychange listener, so after the FIRST guarded render the observer is no longer visibility-aware: it never pauses when document.hidden flips and never fires the snap-on-return reconcile. OGame's 1 Hz event-ticker mutations then drive scheduleRefresh → renderGuarded in a hidden tab, contradicting the module's own contract ('watches while VISIBLE ... zero game-DOM changes while the player is away').
  - **Kierunek naprawy:** Either make VisibilityObserver.disconnect() re-registerable (re-add the visibilitychange listener on the next observe()), or give badges a lighter pause/resume method so guarded() doesn't go through the teardown-grade disconnect().

- [x] **3.4** `src/features/threatHighlight/index.js:304` **[medium/bug]** - endPreview's `if (!visible) hide()` is a dead no-op, and when the feature toggle is OFF while a real attack is inbound the preview leaves a permanent bare 'UNDER ATTACK' banner
  - **Problem:** hide() (line 209) early-returns `if (!visible) return`, so `if (!visible) hide()` can never do anything — the comment's 'otherwise it hides cleanly' fallback never executes from this line. The actual hide is delegated to checkAttack(), but checkAttack returns at line 321 (`if (!settingsStore.get().threatHighlight) return`) BEFORE any hide when the setting is off and #attack_alert says under-attack. The settings-panel Preview button dispatches the test event regardless of the checkbox state (settingsUi/sections/display.js:51-52 — previewing before enabling is the designed flow), so: feature off + genuine inbound attack + Preview ⇒ after PREVIEW_MS the banner stays visible forever (onTick repaints it with lastSummary = {0,0,[]} → bare 'UNDER ATTACK'), even though the user never opted in.
  - **Kierunek naprawy:** In endPreview, when checkAttack declined to keep the alarm (feature off or hidden), force-hide: e.g. call hide() unconditionally when `!settingsStore.get().threatHighlight`, or restructure checkAttack so the feature-off branch also hides a visible banner (mirroring the settings-subscriber path at lines 366-369).

- [ ] **3.5** `src/features/apiContext/index.js:165` **[medium/robustness]** *(ODLOZONE — plik w edycji uzytkownika)* - refreshCache is all-or-nothing: a failure fetching any later feed discards the already-downloaded multi-MB universe.xml, forcing a full re-download on the next page load
  - **Problem:** The six feeds are fetched sequentially with `await fetchApiText(...)` (lines 127-163) mutating a local `cache` object, but `writeApiCache(cache)` runs only once at the end (`if (changed) await writeApiCache(cache)`). If e.g. the players or highscore fetch throws after universe.xml succeeded, the exception propagates out of refreshCache before any write, getContext's caller swallows it (`getContext().catch(() => {})` at line 278), and the successfully fetched weekly multi-MB universe feed is thrown away — every subsequent page load repeats the download until ALL feeds succeed in one pass.
  - **Kierunek naprawy:** Write the cache incrementally (after each successful feed), or wrap each feed fetch in its own try/catch so one flaky feed doesn't discard the others' progress before the single write.

- [x] **3.6** `src/features/readabilityBoost.js:602` **[medium/bug]** - onDomReady restarts the countdown trimmer and movement relabeller without checking the readabilityBoost setting, so the DOM-rewriting observers run even when the feature is toggled off
  - **Problem:** installReadabilityBoost() runs at document_start (content.js:57) BEFORE initSettingsStore() (content.js:107). For a user with readabilityBoost=false the hydration notification fires the subscriber (lines 622-634) which calls retract()+stopCountdownTrimmer()+stopMovementRelabel(). But the DOMContentLoaded listener registered at line 614 invokes onDomReady (lines 602-605), which calls startCountdownTrimmer() and startMovementRelabel() unconditionally — no settingsStore.get().readabilityBoost check. Both starters immediately mutate the page (trimCountdown rewrites '#eventboxFilled .countdown' to 'Done'/'42m 56'; relabelMovementLinks forces 'Fleets:'/'Expos:' labels) and attach MutationObservers that keep rewriting. This contradicts the module's own contract ('The trimmer shares the readabilityBoost toggle — start on enable, disconnect on disable', lines 505-506).
  - **Kierunek naprawy:** Guard onDomReady (or the two start* functions) on settingsStore.get().readabilityBoost so the deferred DOMContentLoaded start respects the hydrated preference.

- [x] **3.7** `src/features/shared/fleetCourier.js:399` **[medium/robustness]** - select() ignores the setTarget RPC result, so a failed target-set can walk the form to fleet2 aimed at a stale target and mislabel the failure.
  - **Problem:** Line 399 is bare `await rpc('setTarget', order.target);` with no ok-check, while the selectShips RPC two lines above is checked (`if (!r || !r.ok) return { ok:false, reason:'selectFailed' }`). If setTarget fails/times out but AGR's fleet1 row still holds coords from a prior pass (the exact back-button re-entry case the code models), continueReady() can already be true → clickContinue proceeds; awaitCheckTarget(order.target) then never matches the old-coord response, the 500 ms fallback (line 444) skips validation, and the mission is armed on the stale target. In the selectMostShips path (line 389-393) setTarget is the first RPC, so an executor failure has no earlier checked call to catch it.
  - **Kierunek naprawy:** Check the setTarget reply like selectShips: `const t = await rpc('setTarget', order.target); if (!t || !t.ok) return { ok:false, reason:'selectFailed' };`.

- [x] **3.8** `src/features/shared/planetList.js:46` **[medium/bug]** - findNextPlanetInList's 'skip' mode silently excludes the first planet (and 'last' mis-orders) when no row carries hightlightPlanet — e.g. whenever the active body is a moon.
  - **Problem:** `const start = activeIdx < 0 ? 0 : activeIdx;` with `lo = active === 'first' ? 0 : 1` means that when `findIndex(p => p.classList.contains(ACTIVE_PLANET_CLASS))` returns -1, 'skip' mode never visits planets[0] — violating the doc's own invariant "Every reachable row is visited exactly once in all three modes" (line 25). On moon pages the row carries hightlightMoon, not hightlightPlanet (galaxyView.js:10-12 documents exactly this divergence), so sendExpedition's findPlanetWithExpSlot(skipCurrent=true) (sendExpedition/domHelpers.js:107) can report no free-slot planet even though planets[0] has room.
  - **Kierunek naprawy:** When activeIdx < 0 there is no active row to skip/defer: treat all modes as a plain full walk (`lo = 0, hi = planets.length - 1`).

- [ ] **3.9** `src/features/apiContext/index.js:129` **[medium/robustness]** *(ODLOZONE — plik w edycji uzytkownika)* - refreshCache persists a zero-planet parse of universe.xml with a fresh fetchedAt, poisoning the occupancy cache for up to 7 days after one degenerate 200 response
  - **Problem:** index.js:127-132: `const u = parseUniverse(await fetchApiText('universe')); cache.universe = { planets: u.planets, timestamp: u.timestamp, fetchedAt: now };` then line 165 `if (changed) await writeApiCache(cache)`. fetchApiText only throws on non-2xx (lib/ogameApi.js:46); domain/apiOccupancy.js:204-218 parseUniverse returns `{ timestamp: undefined, planets: [] }` for ANY 200 body that doesn't match PLANET_TAG_RE (truncated body, interstitial page, API format change) — no throw, no minimum-content validation. TTL.universe is 7 days (line 57), so one bad response makes buildOccupancyIndex see an entirely empty universe (every position 'free', no occupant bands) for the dashboard Scout and colonize picker for a week, with no self-healing. Same pattern applies to the players/highscore feeds (lines 133-158) with shorter TTLs.
  - **Kierunek naprawy:** Treat a parse yielding zero planets/players or a missing root timestamp as a failed fetch: keep the previous feed and don't stamp fetchedAt (so the next load retries).

## Etap 4: Lifecycle, teardown i architektura (medium)

Izolacja bledow instalacji feature-ow, wycieki listenerow przy dispose, naruszenia pure-core i dyscypliny stanu.

Sugerowany commit: `fix(core): install isolation + teardown/layering fixes`

**Notka (2026-07-04):** wszystkie 6 zrobione i scommitowane (workflow: impl→verify→holistic→gate, 6× CORRECT). Dwie uwagi:
(a) **4.4** zmienil sygnature `findNextColonizeTarget` (dodany param `now` na 6. pozycji — po `preferOther`, przed opcjonalnymi; wstawienie po `index` dawalo TS1016) i usunal martwe pole `link` → `test/features/sendColonyHelpers.test.js` ma teraz 13 bledow typecheck (stara arita + `.link`); to rekoncyliacja testow przy release (por. CLAUDE.md, tracked w RELEASE-HANDOFF.md). Produkcyjny `src/` typecheckuje sie czysto.
(b) **4.5** zrobione wariantem behavior-preserving: tylko hoisting stalej `GAME.AGO_FLEET2_MAIN` do gameDom.js, ZERO zmian logiki predykatu (brak dodawania `.off`), zeby nie ruszac runtime alarmu.

- [x] **4.1** `src/content.js:173` **[medium/robustness]** - installDomFeatures has no per-feature error isolation — one throwing install*() silently disables every feature installed after it
  - **Problem:** Lines 173-268 call ~20 install*() functions sequentially inside one plain function with no try/catch. The file's own header (lines 166-172) documents that this exact failure mode already happened once ('badges.js's MutationObserver.observe(document.body) throws ... and aborts the whole bootstrap (every subsequent install would be skipped)'), but the fix only deferred the calls to DOMContentLoaded — the cascade itself remains. OG-E is a parasite on volatile game/AGR DOM: a single game-update-induced throw in an early install (e.g. installColonyRecorder or installBadges) still takes down the FAB buttons, settings panel, alarmClock, sync UI and AGR guard behind it, turning a one-feature breakage into a whole-extension outage with nothing in the console (logger is off by default).
  - **Kierunek naprawy:** Wrap each install call (or iterate an array of installers) in try/catch that routes the error through logger.error, so one broken feature degrades to a one-feature outage instead of aborting the rest of the bootstrap.

- [x] **4.2** `src/features/alarmClock/eventList.js:585` **[medium/async-teardown]** - chrome.storage onChanged listener registered at install is never removed on dispose — the unsubscribe returned by chromeStore.onChanged(onMirror) is discarded.
  - **Problem:** Line 585 is a bare `chromeStore.onChanged(onMirror);` — lib/storage.js:230-244 shows onChanged returns an idempotent unsubscribe that removeListener's the callback, and sync/scheduler.js:1196 captures it (`const unsubStorage = chromeStore.onChanged(...)`). eventList's dispose() (lines 626-636) removes the click listener, observer, clock poll, flip timer and three store subscriptions but has no removal for onMirror, so every install/dispose cycle (incl. _resetEventListAlarmClockForTest) leaks a permanent storage listener: after dispose, any write to ALARM_CLOCK_MIRROR_KEY still fires `void refreshSnapshot().then(() => scheduleRender())` (line 583) — a chrome.storage read plus a write to the module-level `snapshot` — and each reinstall stacks another duplicate listener.
  - **Kierunek naprawy:** Capture the return value (`const unsubMirror = chromeStore.onChanged(onMirror);`) and call it in dispose() alongside unsubPoll/unsubSettings.

- [x] **4.3** `src/features/alarmClock/producer.js:535` **[medium/async-teardown]** - A pending debounced run survives producer.dispose() and executes a full gist+ntfy sync after teardown; on re-install two runs can overlap, defeating the single-writer serialization lock.
  - **Problem:** Install unconditionally arms the debounce (line 532 `scheduleRun();`, 1.5s window) and dispose (lines 535-543) only removes event listeners/store subscriptions — lib/debounce.js is deliberately cancel-less ('no .cancel()', line 11) and neither run() (419-433) nor doRun() (278-414) re-checks `installed`. So a dispose inside the debounce window still performs the whole read-state → poll-ntfy → PATCH-gist round-trip post-teardown. Worse, the `running`/`rerunQueued` lock is closure-scoped per install, so after dispose→re-install the stale run and the new producer's run can PATCH the per-universe gist file concurrently — the exact last-write-wins loss the lock comment (lines 260-268) says must be serialized.
  - **Kierunek naprawy:** Guard the debounced callback on the install sentinel (e.g. `debounce(() => { if (installed) void run(); }, DEBOUNCE_MS)` plus an installed check inside run after each await), or give debounce a cancel and invoke it in dispose.

- [x] **4.4** `src/features/sendColony/pure.js:193` **[medium/layering]** - The 'pure' core reads location.href (and Date.now at line 162) inside findNextColonizeTarget, and the `link` it builds from location.href is dead — no caller uses it.
  - **Problem:** Line 193: `const link = ingameComponentUrl(location.href, 'fleetdispatch', ...)` inside findNextColonizeTarget, in a file whose header states 'No DOM reads ... If you find yourself wanting to import document / window / location here, STOP' (lines 38-42). Both consumers discard `link`: derive() copies only galaxy/system/position (lines 543-549, 568-574) and index.js onSendClick uses only candidate.galaxy/system/position (lines 499-556); grep shows no other `.link` reader. Additionally line 162 calls Date.now() for the inFlight filter even though derive receives env.now for exactly this purpose (DeriveEnv doc lines 457-458) — sibling pickCandidateInView correctly takes `now` as a parameter (line 257).
  - **Kierunek naprawy:** Delete the `link` field (and the ingameComponentUrl/location.href call) from findNextColonizeTarget, and thread `now` in as a parameter like pickCandidateInView does, restoring the pure-core contract.

- [x] **4.5** `src/features/alarmClock/guardian.js:276` **[medium/layering]** - AGR selector '#ago_fleet2_main' is read by literal id in three modules across two features instead of living in lib/gameDom.js, and guardian duplicates agrRoutine's readiness predicate minus its .off guard.
  - **Problem:** guardian.js:276 `document.querySelector(GAME.FD_DISPATCH) && document.getElementById('ago_fleet2_main')`; the same literal appears in features/sendExpedition/index.js:440 and features/shared/agrRoutine.js:93 and :116 — an AGR selector read by 2+ features, which the gameDom invariant requires to be hoisted (gameDom.js already hosts the other AGR ids: AGO_GALAXY, AGO_MENU_BUTTON, etc.), so an AGR rename is currently a 3-file hunt. guardian's inline copy is also agrRoutine.js dispatchReady() (111-118) without the FD_DISABLED_CLASS '.off' check that dispatchReady's comment calls the bug it guards against (harmless today only because dispatchPrepared re-checks internally).
  - **Kierunek naprawy:** Add e.g. GAME.AGO_FLEET2_MAIN to lib/gameDom.js and use it at all three sites; or export an `isFleet2Up()` helper from features/shared/agrRoutine.js and have guardian/sendExpedition call it.

- [x] **4.6** `src/features/traderMenuHighlight.js:736` **[medium/state-hygiene]** - Passive-scan writes to synced DailyState keys bypass state/dailyActions' readDailyState/writeDailyState (duplicating the read logic with divergent defaults) and skip the DAILY_STATE_CHANGED_EVENT dispatch, so cross-device clearing is delayed
  - **Problem:** markImportTaken (lines 731-740), scanTraderSubpages (lines 783, 803) and onBidPlaced (line 925) call safeLS.set(...) directly on TRADER_* keys, and readState (lines 600-606) re-implements per-key readers (readAuctionBidAt/readAuctionQuietUntil/readImportNextAt, lines 531-562) that parallel state/dailyActions.js readDailyState (lines 69-76) with different missing-value semantics (null here vs 0 there). The refresh() path that calls the passive-scan stamps (lines 916-921) never dispatches DAILY_STATE_CHANGED_EVENT — unlike the XHR paths onBidPlaced/onImportTraded (lines 925-936) and the sibling features rewardingWatcher.js:105-106 / artifactShopWatcher.js:171-172, which all pair writeDailyState with the event. sync/scheduler.js:1156-1160 relies on that event to scheduleUpload() for the synced dailyStatePerUniverse slot (scheduler.js:684-689), and state/dailyActions.js:51-54 documents TRADER_IMPORT_NEXT_KEY as synced precisely 'so taking the offer on one device clears the nudge on another'. An overlay-detected import/auction stamp therefore sits locally until some unrelated sync trigger fires, the DailyState keys have two independent write paths, and the CLAUDE.md rule is that features never write backing localStorage keys directly — the exception is the key-owner module itself, not its callers.
  - **Kierunek naprawy:** Route the passive-scan stamps through writeDailyState({traderImportNextAt|traderImportDay|traderAuctionQuietUntil}) and dispatch DAILY_STATE_CHANGED_EVENT when a value actually changed (the writes are already change-gated, so this cannot spam); eliminate the three duplicate readers (keeping the feature-local IMPORT_MODE_KEY/IMPORT_EVENT_SEEN_KEY as-is).

## Etap 5: Martwy kod - usuwanie

Same delecje - git przechowa historie. Przed kazda delecja ponow grep upewniajacy sie, ze nic nie wola (takze uzycia stringowe). UWAGA: #ago_routine_7 i komentarze game-knowledge sa chronione; _reset*ForTest i dispose*Store to sankcjonowane afordancje.

Sugerowany commit: `refactor: remove dead code (legacy strategy engine, settingsUi controls, dead exports)`

**Notka (2026-07-04):** 13/15 zrobione i scommitowane (workflow: 11 rozlacznych bundli → 11× verify CORRECT + holistic CORRECT; gate build+lint zielone, typecheck czerwony TYLKO w test/** — martwe testy usunietych eksportow, rekoncyliacja przy release per CLAUDE.md). **5.12 (spyMissionUrl) + 5.13 (DEFENSE_CATALOG)** dokonczone pozniej (2026-07-04, osobny commit) — pliki byly czyste (ogameUrl.js/unitCosts.js nie w WIP), symbole wciaz martwe, usuniete. Uwaga procesowa: bundle 5.2 (buttons) usunal tez osierocone eksporty FOCUS_* z sendExpedition/pure.js, kolidujac plikowo z bundlem 5.10 (POLL) — 5.10 zwrocil null (StructuredOutput retry cap), ale obie delecje wyladowaly spojnie (potwierdzone: src/ typecheckuje sie czysto, verify 5.10 = CORRECT).

- [x] **5.1** `src/features/settingsUi/controls.js:698` **[medium/dead-code]** - Six control flavours (text, password, select, duration, button, asyncStatus incl. the whole secret/eye/copy stack and refresh-event machinery) are dead code since the settings-to-dashboard move
  - **Problem:** Grep over src/features/settingsUi/sections/ shows the live SECTIONS use only type 'checkbox', 'range', 'radio', and 'static' (floatingButton.js:17-18, expeditions.js:18-21, display.js:20-52, sync.js:26, alarmClock.js:31). Nothing constructs 'text'/'password'/'select'/'duration'/'button'/'asyncStatus' options anywhere in src/, yet controls.js carries buildInputControl, buildSelectControl, buildDurationControl, buildButtonControl, buildAsyncStatusControl, buildSecretStack (576-622, eye/copy buttons and the "Copied!"/1200 ms reset timer), paintSecretValue (223-225), refreshAsyncStatus, asyncStatusState (139-210), asyncRefreshEventBound (which registers never-removed document listeners), plus the maskTopic import from sync/alarmClock.js (line 33) and parseDuration/formatDuration from domain/duration.js (line 32) — roughly 350+ lines shipped in the content bundle solely to support flavours the panel no longer renders. The topic row moved to the dashboard (sections/alarmClock.js:11 says so), whose live, drifted implementation is features/dashboard/alarmClock.js:119-190 (adds reveal-on-copy-failure, lines 140-145).
  - **Kierunek naprawy:** Delete the unused builders, the asyncStatus/secret machinery, the corresponding branches in syncInputsFromState, and the now-unused imports; shrink the SettingsOption type union to the flavours actually rendered — dashboard/alarmClock.js is the single live implementation. Git history keeps the old code if the dashboard move is ever reverted.

- [x] **5.2** `src/features/shared/button.js:479` **[medium/dead-code]** - The entire focus-persistence pipeline is dead: no user gesture can ever focus a zone, so focusValue/focusKey is never written and never restored.
  - **Problem:** createButton sets `btn.tabIndex = -1` (line 236) and `half.tabIndex = -1` (line 286) on every zone, removing keyboard focusability; buttonChrome.js wireZoneTap (line 644) adds `zone.addEventListener('mousedown', (e) => e.preventDefault())`, which blocks click-focus. installFocusPersist (draggableButton.js:273) only writes `safeLS.set(focusKey, focusValue)` inside a 'focus' listener, and its restore branch (line 281) only fires when that stored value matches — but the only `.focus()` call on these buttons in all of src IS that restore (grep confirms). Six features (dailyRun, sendColony, sendSpy, sendExpedition, sendLifeform) still pass focusValue believing it works.
  - **Kierunek naprawy:** Either delete focusValue/focusKey/focusRestoreDelay plumbing from ZoneConfig + installFocusPersist and its six call sites, or restore focusability (drop tabIndex=-1 / the mousedown preventDefault) if the collect-walk focus restore is still wanted.

- [x] **5.3** `src/features/shared/buttonChrome.js:280` **[medium/dead-code]** - The .oge-host.oge-charging / .oge-host.oge-fired CSS block (plus @keyframes, data-flag rules and reduced-motion overrides) is dead — no JS ever adds those classes or sets --charge, and the comment falsely claims JS drives them.
  - **Problem:** BUTTON_CHROME_CSS styles '.oge-host.oge-charging' (280) and '.oge-host.oge-fired' (288, keyframes 289-291, reduced-motion 330-331) plus [data-flag~=…] rules and the oge-alert keyframes (299-311), and the comment at 279 claims '--charge (0→1) is set by JS'. Grep across all of src: the only occurrences of oge-charging/oge-fired/--charge are these selector lines — no classList.add, no setProperty('--charge'), no data-flag writer (dailyRun/index.js:623 explicitly states it does NOT set data-flag). The actual hold-to-confirm feedback is the '.oge-ring-charge' SVG arc animated via stroke-dashoffset in button.js:367-396. Relatedly, the comment at line 22 references a '.oge-exp-label' span that exists nowhere in src.
  - **Kierunek naprawy:** Remove the oge-charging/oge-fired/data-flag rules, the oge-fire/oge-alert keyframes and their reduced-motion overrides from BUTTON_CHROME_CSS (shipped to every page), and fix the false --charge comment, the stale .oge-exp-label mention, and the stale dailyRun comment at index.js:613 that claims the host carries data-flag.

- [x] **5.4** `src/sync/merge.js:308` **[medium/dead-code]** - mergePlayers and mergeOwnProfile (plus playersSlotHasData / ownProfileHasData in sync/scheduler/pure.js:164/175) are dead exports — leftovers of the removed §4b players/own-profile sync
  - **Problem:** scheduler.js:851 comment: 'Galaxy scans / players / own-profile are no longer synced (§4b)' and :896-897 'playersPerUniverse / ownProfilePerUniverse are intentionally omitted'. SYNC_SLOTS (scheduler.js:661-714) contains no players/ownProfile slot, and no hasData/merge wiring references these four exports — mergePlayers (merge.js:308), mergeOwnProfile (merge.js:339), playersSlotHasData (scheduler/pure.js:164), ownProfileHasData (scheduler/pure.js:175) are referenced only by tests and by stale JSDoc links in gist.js:184/190. domain/players.mergePlayerMeta and state/players.js remain alive (in-game write path the dashboard reads by raw key) — only the sync-side helpers are orphaned. mergeScans by contrast is still live (features/dashboard/io.js:49).
  - **Kierunek naprawy:** Delete the four dead exports and their tests at the next release-time test reconciliation; also update the stale gist.js payload doc (lines ~184-190) that still describes players/ownProfile merge semantics.

- [x] **5.5** `src/domain/regions.js:504` **[medium/dead-code]** - The entire legacy strategy engine (STRATEGIES, sortRegionsByStrategy, scoreForStrategy, expansionFactor, rankRelative, systemIntentHeat) is dead code — superseded by domain/zoneScore.js, zero callers in src/
  - **Problem:** STRATEGIES (line 504) is referenced only by sortRegionsByStrategy (656), which no src file imports (grep across src incl. HTML: only a comment in zoneScore.js:2, whose header states it 'Replaces the legacy 6-preset × 9-weight strategy dot-product (regions.js STRATEGIES)'). systemIntentHeat (1215) also has zero non-comment references (only a JSDoc link in features/dashboard/palette.js:144). Private helpers expansionFactor (551) and scoreForStrategy (608) are reachable only from these dead exports; rankRelative (565) is called only from the two dead functions (lines 612 and 1222). The sole regions.js consumer, features/dashboard/freeStreak.js:41-47, imports only findBestRegions, findFreeSystems, findNeighbourhoodCandidates, spaceOutCandidates, MIN_REGION_LENGTH. STRATEGIES has zero test uses; sortRegionsByStrategy/systemIntentHeat are test-only. The STRATEGIES doc claim 'Exported so the UI can build a <select> from them' is stale — no UI does.
  - **Kierunek naprawy:** Delete STRATEGIES, sortRegionsByStrategy, scoreForStrategy, expansionFactor, systemIntentHeat and the now-orphaned rankRelative plus the SortOptions typedef (StrategyWeights must stay — findNeighbourhoodCandidates' playerHarm still consumes it) and their tests at the next release-time test reconciliation; fix the stale doc pointer at line 875 ('sorts and renders through the exact same sortRegionsByStrategy / table code') to reference annotateAndSortByZone. scoreRegion and the region finders stay — they are alive.

- [x] **5.6** `src/state/galaxyScanConfig.js:143` **[medium/dead-code]** - stampGalaxyScanConfigChanged (and its twin stampAlarmClockConfigChanged in state/alarmClockConfig.js:142) has no production caller — the config edit path moved to the dashboard, which writes the Ts key directly.
  - **Problem:** Grep across src finds only the definition (galaxyScanConfig.js:143) and a cross-reference comment in alarmClockConfig.js:137; both stamps are test-only. The function's own doc says 'The dashboard writes the same key directly on save' — and it does (e.g. features/dashboard/scanConfig.js writes the Ts + syncRequest keys itself). The private flushGalaxyScanConfigStore (130) is reachable only from the dead stamp. By contrast stampDailyRunRoutesChanged is still called in-game, so these two are asymmetric leftovers of the settings-to-dashboard move.
  - **Kierunek naprawy:** Delete both stamp functions and the private flush helper each drags along; keep the Ts key constants (still used by the dashboard write path).

- [x] **5.7** `src/domain/traderClock.js:26` **[low/dead-code]** - parseClockTime is exported but has no production consumer — the 'come back at HH:MM' overlay approach the module header describes was superseded by countdown parsing + 4-hour slots
  - **Problem:** Grep over src/ for parseClockTime matches only its definition (domain/traderClock.js:26). traderMenuHighlight.js imports only nextDailyOccurrence/slotStartMs/nextSlotStartMs from this module (line 98) and derives the quiet window from parseTraderCountdown (domain/traderCountdown.js, traderMenuHighlight.js:795-806) and the 6x re-arm from slot boundaries — never from the overlay's HH:MM text. The module header (lines 3-11) still presents the HH:MM extraction as the module's purpose, which misdescribes the shipped design.
  - **Kierunek naprawy:** Remove parseClockTime (and its typedef reference) or, if it is kept deliberately for a planned use, correct the module header so it no longer claims the overlay-time path is what the trader feature does.

- [x] **5.8** `src/sync/gist.js:509` **[low/dead-code]** - validateToken is dead-for-production — the dashboard reimplements the probe locally because it cannot read the game origin's localStorage.
  - **Problem:** Grep across src: only the definition (gist.js:509) and a comment in features/dashboard/settingsControls.js:75-77 explaining why the dashboard's own validateGistToken (settingsControls.js:82) is used 'not via gist.js validateToken'. Used only by tests (5 refs). The old in-game Settings Token row that consumed it is gone.
  - **Kierunek naprawy:** Delete validateToken and its tests, or move the probe into a shared token-parameterised helper both origins can call — currently the two implementations can drift.

- [x] **5.9** `src/features/dashboard/io.js:301` **[low/dead-code]** - triggerSync is dead-for-production — five call sites hand-roll its exact body instead of calling it.
  - **Problem:** triggerSync (io.js:301-302) = chromeStore.set(syncRequestKeyFor(universeId), Date.now()). Grep: no src caller (test-only, 3 refs), while the identical expression is inlined at features/dashboard/alarmClockConfig.js:834, routes.js:160 and 581, scanConfig.js:164, and settingsControls.js:71.
  - **Kierunek naprawy:** Either delete triggerSync, or (better) adopt it at the five inline call sites so the force-sync contract lives in one place.

- [x] **5.10** `src/features/sendExpedition/pure.js:78` **[low/dead-code]** - POLL_TIMEOUT_MS and POLL_INTERVAL_MS are dead — AGR-readiness polling moved to features/shared/agrRoutine.js with its own identical constants.
  - **Problem:** POLL_TIMEOUT_MS (78) and POLL_INTERVAL_MS (81) have zero non-comment references in src (POLL_INTERVAL_MS has zero test refs too; POLL_TIMEOUT_MS is mentioned only in a stale comment at bridges/eventBoxObserver.js:16). agrRoutine.js:21-23 defines ROUTINE_POLL_TIMEOUT_MS=15_000 / ROUTINE_POLL_INTERVAL_MS=300 with the same values and is what the routine polling actually uses (agrRoutine.js:70/96/160-161).
  - **Kierunek naprawy:** Delete both constants from sendExpedition/pure.js and repoint the eventBoxObserver.js:16 comment at ROUTINE_POLL_TIMEOUT_MS.

- [x] **5.11** `src/sync/ntfyReconciler.js:371` **[low/dead-code]** - WAVE_PRIORITY and ADHOC_PRIORITY are dead — push priority now comes from the alarm-clock template, and the JSDoc still claims the flat constants are used.
  - **Problem:** WAVE_PRIORITY (371) and ADHOC_PRIORITY (372) appear in code only at their definitions (test-only otherwise); every postMessage call passes priority from template.priority (lines 610, 731, 818) or a literal 5 (882). The docs at lines 567 and 779 still say slots are posted 'at flat WAVE_PRIORITY / ADHOC_PRIORITY', which no longer describes the code.
  - **Kierunek naprawy:** Delete the two constants and correct the two JSDoc mentions to say priority comes from the normalized template.

- [x] **5.12** `src/domain/ogameUrl.js:63` **[low/dead-code]** - spyMissionUrl has zero references anywhere — including tests.
  - **Problem:** Grep across src (JS+HTML) and test/: the only occurrence of spyMissionUrl is its definition at ogameUrl.js:63. ogameUrlBase and ingameComponentUrl in the same module are alive. This looks staged for the Targets tab's pending 'deep-link spy buttons' work (the doc references docs/fair-play.md and the fleetdispatch pre-arm contract).
  - **Kierunek naprawy:** If the deep-link spy button lands soon, keep it and wire it up; otherwise delete it — an untested URL builder for a fragile game contract will silently rot.

- [x] **5.13** `src/domain/unitCosts.js:82` **[low/dead-code]** - DEFENSE_CATALOG has zero references anywhere — including tests.
  - **Problem:** Grep across src and test/: DEFENSE_CATALOG appears only at its definition (unitCosts.js:82-91). Its doc says the dashboard needs static defense names for a per-unit breakdown UI, but no such UI reads it; UNIT_COSTS/resourceValueOf/sumResourceValue in the same file are alive (used internally and by the targets estimate).
  - **Kierunek naprawy:** Delete the DEFENSE_CATALOG binding only — keep UNIT_COSTS and the file's reverse-engineered cost/points commentary intact (protected game knowledge per CLAUDE.md carve-out).

- [x] **5.14** `src/features/dashboard/palette.js:140` **[low/dead-code]** - UNSCANNED_BORDER has zero references anywhere — including tests.
  - **Problem:** Grep across src (incl. dashboard.html) and test/: the only occurrence is the definition at palette.js:140. The companion UNSCANNED_COLOR (134) is alive; only the border constant lost its consumer (the map pixels no longer draw a per-pixel border).
  - **Kierunek naprawy:** Delete the constant and its doc block.

- [x] **5.15** `src/domain/duration.js:189` **[low/dead-code]** - humanizeOffsetShort and humanizeReturnOffsetShort are dead-for-production — the chip-style schedule editor they were written for only uses the long formatters.
  - **Problem:** Both exports (lines 189, 207) have zero non-comment references in src (test-only: 8/7 refs). The dashboard offset editor consumes only the long forms: makeOffsetEditor takes a single previewLong option, fed humanizeReturnOffset / humanizeArrivalOffset / humanizeOffset at features/dashboard/alarmClockConfig.js:455, 463, 492 — no short variant is passed anywhere.
  - **Kierunek naprawy:** Delete the two short formatters and their tests, or wire them into the chip labels if the compact form is still wanted — as-is the 'the two never drift' doc guarantee guards code nobody renders.

## Etap 6: Duplikacja i centralizacja gameDom

Konsolidacja kopii do sankcjonowanych domow (lib/, domain/, features/shared/, lib/gameDom.js). Przy driftach najpierw ustal, ktory wariant jest poprawny, potem ujednolic. Pamietaj: selektor uzywany przez JEDEN feature zostaje lokalny.

Sugerowany commit: `refactor: consolidate duplicated helpers + centralize shared game selectors`

- [ ] **6.1** `src/features/alarmClock/eventList.js:92` **[medium/duplication]** - MISSION_NAMES map and the push-label builder (labelFor / fleetSaveLabelFor) are duplicated verbatim between two files of the same alarmClock feature.
  - **Problem:** eventList.js:92-95 and fleetSaveScan.js:16-20 carry the identical mission-id→English-name map (reverse-engineered game knowledge), and eventList.js:203-210 `labelFor` is byte-equivalent to fleetSaveScan.js:136-142 `fleetSaveLabelFor` (same mission lookup, same isReturn → COORDS_ORIGIN/COORDS_DEST landing-point rule, same `${mission} → [${landing}]` format; fleetSaveScan.js:130 admits "Mirrors the ad-hoc labeller in eventList.js"). eventList already imports fleetRowMeta from ./fleetSaveScan.js (line 77), so there is no import obstacle — the copies can silently drift (e.g. a new mission id added to one table only), making FS pushes and ad-hoc pushes name the same fleet differently.
  - **Kierunek naprawy:** Keep one copy: move MISSION_NAMES to domain/ (it is pure game knowledge; threatHighlight/pure.js:14 already cross-references it) and have eventList.js import `fleetSaveLabelFor` (renamed to a neutral fleetLabelFor) instead of re-declaring both.

- [ ] **6.2** `src/features/targetsIngest/index.js:20` **[medium/duplication]** - OGame-native '.rawMessageData' messages-page selector is duplicated in two features instead of living in lib/gameDom.js
  - **Problem:** targetsIngest/index.js:20 defines RAW_SELECTOR = '.rawMessageData' and traderMenuHighlight.js:214 independently defines MSG_RAW_DATA_SEL = '.rawMessageData' (plus MSG_ROW_SEL='.msg' and the data-raw-timestamp attribute at lines 213-215); both parse the game's messages UI. lib/gameDom.js (read in full) has no entry for it, yet its scope rule says OGame selectors read by two or more features belong there — a game rename of the rawMessageData block would now silently break target ingestion and the trader 6x auto-switch in two separate places.
  - **Kierunek naprawy:** Hoist the rawMessageData selector (and arguably the .msg row selector / data-raw-* attribute prefix shared by both parsers) into GAME in src/lib/gameDom.js and import it from both features.

- [ ] **6.3** `src/features/badges/index.js:120` **[medium/duplication]** - Game-native event-row ownership classes '.hostile' / '.friendly' are read raw by three features but are not centralized in lib/gameDom.js
  - **Problem:** badges/index.js:120 (row.querySelector('.hostile')), threatHighlight/index.js:170 (row.querySelector('.hostile')), and alarmClock/fleetSaveScan.js:125 (isOwnFleet = row.querySelector('.friendly')) all read OGame's fleet-row ownership classes; readabilityBoost.js:261-281 additionally hard-codes both classes in injected CSS. lib/gameDom.js centralizes the surrounding event-row selectors (EVENT_FLEET_ROWS, COORDS_DEST, ORIGIN_FLEET...) but not these classes, so a game rename would silently break hostile detection for badges, the under-attack banner, and FS-candidate scanning in three files at once — exactly the multi-feature fragility gameDom exists to collapse to a one-line fix.
  - **Kierunek naprawy:** Add e.g. GAME.ROW_HOSTILE = '.hostile' / GAME.ROW_FRIENDLY = '.friendly' (bare class constants like ACTIVE_PLANET_CLASS if classList checks are needed) to src/lib/gameDom.js and use them at the three read sites.

- [ ] **6.4** `src/features/badges/index.js:82` **[medium/duplication]** - The dense-coords normalizer is copy-pasted six times across five features, with a seventh drifted variant in sendExpedition.
  - **Problem:** Byte-identical `(s || '').replace(/[\s[\]]/g, '')` is defined as `denseCoords` in badges/index.js:82, alarmClock/eventList.js:182, alarmClock/fleetSaveScan.js:23, threatHighlight/index.js:129-130, and as `dense` in alarmClock/guardian.js:102 and manualFsMark/index.js:90. sendExpedition/pure.js:145 has a drifted grammar for the same job: `stripBrackets = (raw) => (raw ?? '').trim().replace(/^\[|]$/g, '')` (outer brackets only, no inner-whitespace strip). All seven produce the `g:s:p` keys that flow into shared persisted state (fleetSaveSet bodyKeys, guardian coords), so grammar drift silently breaks cross-feature key equality.
  - **Kierunek naprawy:** Hoist one pure `denseCoords(text)` into domain/ (e.g. next to domain/bodies.parseKoords) and import it everywhere; fold sendExpedition's stripBrackets into it so all key producers share one grammar.

- [ ] **6.5** `src/features/alarmClock/fleetSaveScan.js:42` **[medium/duplication]** - Body-identity key (`g:s:p:type`) and landing-leg resolution are re-implemented byte-identically in three features that must agree on the key format.
  - **Problem:** fleetSaveScan.js:42-47 `landingBodyKeyOf` self-documents as "Byte-identical to `features/badges/pure.js::bodyKey`" (badges/pure.js:103 `bodyKey = (coords, type) => `${coords}:${type}``, itself annotated "byte-identical to domain/bodies identity"); manualFsMark/index.js composes the same key inline three times (lines 120, 128, 130: `${metaCoords}:${isMoon ? 3 : 1}`, `${coords}:1`, `${coords}:3`). The moon-vs-planet probe `figureType` is also duplicated (badges/index.js:98-101 vs fleetSaveScan.js:32). All copies key into state/fleetSaveSet.js entries (fleetSaveSet.js:60 just stores `e.bodyKey` strings), so any divergence puts FS markers/pushes on the wrong body with no error.
  - **Kierunek naprawy:** Move `bodyKey(coords, type)` (and the return-leg→origin / outbound→dest landing rule as a pure helper) into domain/bodies.js — the file the comments already name as the identity's source of truth — and import it from badges, alarmClock, and manualFsMark.

- [ ] **6.6** `src/features/dashboard/index.js:456` **[medium/duplication]** - Three different `<universeId>:oge_*` discovery/parsing implementations coexist in the dashboard feature with divergent policies.
  - **Problem:** dashboard/index.js:456-474 `discoverUniverses` scans chromeStore keys against a hand-maintained whitelist of three suffixes (HISTORY/SCANS/GALAXY_SCAN_CONFIG — the comment shows the config suffix had to be added later); dashboard/settingsControls.js:66-68 `requestSyncAll` inlines a different scan accepting ANY `<id>:oge_*` key; dashboard/syncInventory.js:103-109 `parsePerUniverseKey` encapsulates that same any-oge_ parse (unexported). Result: a universe holding only e.g. `oge_alarmClockConfig` gets sync pokes and inventory rows but never appears in the universe selector.
  - **Kierunek naprawy:** Export `parsePerUniverseKey` from syncInventory.js and build both `discoverUniverses` and `requestSyncAll` on it (with an optional suffix filter if the selector's narrower policy is intentional — then the policy difference becomes explicit instead of accidental).

- [ ] **6.7** `src/features/badges/index.js:485` **[low/layering]** - badges hard-codes getElementById('planetList') in two spots even though GAME.PLANET_LIST centralizes that selector and badges already imports GAME
  - **Problem:** badges/index.js imports GAME (line 52) and uses GAME.SMALL_PLANET / GAME.EVENT_CONTENT etc. throughout, but positionHelpChip (line 485) and attachObserver/legend-anchor code (line 714) call document.getElementById('planetList') with the raw string. `GAME.PLANET_LIST: '#planetList'` is defined in src/lib/gameDom.js:53 and consumed by src/features/planetBarCapture.js:246 — so this is a 2+-feature game-DOM selector that the gameDom invariant says must be read from the single source of truth; a game rename would be 'fixed' in gameDom yet still break these two badge code paths, defeating the one-line-fix contract.
  - **Kierunek naprawy:** Use `document.querySelector(GAME.PLANET_LIST)` (or derive the id from the constant) in both spots.

- [ ] **6.8** `src/features/shared/fleetDispatcherCache.js:58` **[low/duplication]** - bootstrap() aliases the live window.fleetDispatcher object as the 'snapshot' while the sibling seed in fleetCourier.js copies and normalizes — divergent duplicate logic with different semantics.
  - **Problem:** `snapshot = /** @type {FleetDispatcherSnapshot} */ (liveFd);` stores a live, mutable reference whose fields are not normalized, whereas fleetCourier.js:631-641 seeds the same concept by building a fresh object with `fleetCount: Number(fd.fleetCount) || 0` etc. Consumers of the cache (e.g. isFleetCapReached reading fleetCount) get values that mutate under them and may not be numbers, unlike every event-delivered snapshot.
  - **Kierunek naprawy:** Have bootstrap() build the same normalized copy fleetCourier does (ideally share one seedFromLiveFd helper between the two modules).

- [ ] **6.9** `src/features/dailyRun/index.js:333` **[low/duplication]** - safeWrite/clearRedirectStash hand-roll the try/catch localStorage guards that lib/storage.safeLS exists to provide
  - **Problem:** dailyRun/index.js:333-348 wraps raw `localStorage.setItem/removeItem(DAILY_RUN_REDIRECT_KEY, ...)` in bespoke try/catch blocks with the same private-mode/quota rationale that lib/storage.js:1-10 documents as safeLS's entire reason to exist (safeLS.set is exactly this try/catch, lib/storage.js:67-73) — and the read side of the very same key already uses safeLS (bridges/deployRedirect.js:149-153 `safeLS.get`/`safeLS.remove`). These are the only raw localStorage calls in the feature layer; two idioms for the same key means the wrapper's future hardening (logging, key policy) would miss this write path.
  - **Kierunek naprawy:** Import safeLS in dailyRun/index.js and replace safeWrite/clearRedirectStash with `safeLS.set(DAILY_RUN_REDIRECT_KEY, url)` / `safeLS.remove(DAILY_RUN_REDIRECT_KEY)`.

- [ ] **6.10** `src/features/dailyRun/domHelpers.js:42` **[low/duplication]** - parseCoordsText is a byte-identical duplicate of domain/bodies.parseKoords, which was created precisely as its canonical mirror.
  - **Problem:** dailyRun/domHelpers.js:42-51 and domain/bodies.js:61-70 are the same function (same `/(\d+):(\d+):(\d+)/` match, same `{galaxy, system, position}` result). bodies.js:54-56 even documents it: "Mirrors `features/dailyRun/domHelpers.parseCoordsText` but lives here in `domain/`". Features may import domain, so the feature-side copy no longer has a reason to exist.
  - **Kierunek naprawy:** Delete parseCoordsText from dailyRun/domHelpers.js and import parseKoords from domain/bodies.js; update the mirror comment in bodies.js.

- [ ] **6.11** `src/features/alarmClock/fleetSaveScan.js:109` **[low/duplication]** - shipCountOf (locale-independent digits-strip ship count from the detailsFleet cell) is duplicated between alarmClock and badges.
  - **Problem:** fleetSaveScan.js:109-113 (`row.querySelector(GAME.DETAILS_FLEET)?.textContent` → `txt.replace(/\D/g, '')` → parseInt) and badges/index.js:85-88 (`shipCountOf` on the same cell text, identical digits-strip logic, fed at index.js:130 from GAME.DETAILS_FLEET). Both drive fleet-save classification against the same threshold state, so the counters must stay in lockstep; today they only agree by copy-paste.
  - **Kierunek naprawy:** Extract one pure `shipCountFromText(text)` into domain/ (or a features/shared event-row reader alongside denseCoords/figureType) and import it from both features.

- [ ] **6.12** `src/sync/ntfyReconciler.js:238` **[low/duplication]** - The epoch-seconds → locale HH:MM clock formatter is duplicated byte-for-byte between sync and the alarmClock feature.
  - **Problem:** sync/ntfyReconciler.js:238-239 `clock = (epochSec) => new Date(epochSec * 1000).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })` and features/alarmClock/eventList.js:185-186 `fmtClock` are identical. eventList's doc comment calls it "the one clock format every tooltip uses", and the reconciler renders the same format into push wildcard values ({returnTime}/{arrivalTime}) — the tooltip and the push should agree by construction, not by parallel copies.
  - **Kierunek naprawy:** Hoist one `clockHHMM(epochSec)` into domain/ (pure) or lib/ and import it from both ntfyReconciler and eventList.

- [ ] **6.13** `src/features/sendColony/index.js:160` **[low/duplication]** - The shared focus-persistence contract string 'oge_focusedBtn' is re-declared in five features on top of the shared button default.
  - **Problem:** `const FOCUS_KEY = 'oge_focusedBtn'` appears in sendColony/index.js:160, dailyRun/index.js:98, sendSpy/index.js:67, sendLifeform/index.js:77, and sendExpedition/pure.js:50 — and features/shared/button.js:127 already defaults `focusKey` to the same literal. The key is a cross-feature contract (all floating buttons coordinate exclusive focus through it via safeLS), so a typo in any one copy silently breaks focus exclusivity for that button only.
  - **Kierunek naprawy:** Either drop the per-feature constants and rely on button.js's default, or export the literal once (lib/storageKeys.js is the sanctioned home for shared key strings) and import it.

- [ ] **6.14** `src/features/shared/unifiedFab.js:160` **[low/duplication]** - Two shared modules re-implement lib/dom.injectStyle, and one copy lacks its document_start head-fallback.
  - **Problem:** unifiedFab.js:160-166 `ensureCss` and buttonChrome.js:339-345 `installButtonChrome` both re-implement the exact idempotent-by-id `<style>` injection that lib/dom.js:159-166 `injectStyle(id, css)` provides (and which badges, eventList, threatHighlight etc. already import). The unifiedFab copy appends to `document.head` with no `?? document.documentElement` fallback, so it has silently dropped the document_start guarantee injectStyle documents.
  - **Kierunek naprawy:** Replace both local implementations with `injectStyle(STYLE_ID, CSS)` from lib/dom.js.

- [ ] **6.15** `src/features/dashboard/freeStreak.js:830` **[low/duplication]** - Two compact-magnitude number formatters in the dashboard have drifted apart (k vs K, missing B tier, different precision).
  - **Problem:** freeStreak.js:830-831 `fmtPts` renders "1.2M / 340k / 850" (no billions tier, lowercase k), while targets.js:47-54 `compact` renders "4.57B / 47.9M / 880K" (B tier, uppercase K, different decimal rules). Both compact points/magnitude values for dashboard stat displays, so the same number formats differently depending on which tab the user is on.
  - **Kierunek naprawy:** Extract one shared compact formatter (domain/ or a dashboard-local shared module — both files are in the same feature so a plain import works) and parameterise precision if the column-width needs genuinely differ.

- [ ] **6.16** `src/features/eventMenuHighlight.js:255` **[low/duplication]** - The observer target hard-codes getElementById('menuTable') while the very same feature reads the centralized GAME.MENU_TABLE selector three lines of logic earlier — a game rename now needs two fixes, not the promised one
  - **Problem:** Line 196 queries `${GAME.MENU_TABLE} .menubutton.premiumHighligt` (gameDom.js:228 defines MENU_TABLE: '#menuTable'), but line 255 reads `document.getElementById('menuTable') ?? document.body` with the raw id string. If OGame renames the node, the highlight query at 196 is fixed by the one-line gameDom edit, but the observer at 255 silently falls back to observing the whole body subtree — same class of drift as the already-flagged badges getElementById('planetList') vs GAME.PLANET_LIST.
  - **Kierunek naprawy:** Derive the id from the shared constant (e.g. document.querySelector(GAME.MENU_TABLE)) so the observer target and the highlight query cannot diverge.

## Etap 7: Drobne bugi, race-y i teardown (low)

Niezalezne drobiazgi. Pozycja 7.8 (sendColony view-priority) wymaga decyzji projektowej - skonsultowac z uzytkownikiem przed implementacja.

Sugerowany commit: `fix(features): minor races, teardown leaks, feedback glitches`

- [ ] **7.1** `src/features/dashboard/index.js:711` **[low/bug]** - Two quick universe-selector changes interleave loadWatched: each call clears watchedPlayers synchronously, then both resolved gets add their players, leaving the in-memory set a union of two universes — which the next writeWatchConfig persists into the new universe's key
  - **Problem:** loadWatched (lines 711-730) does `watchedPlayers.clear()` synchronously, then `await chromeStore.get(watchListKeyFor(selectedUniverseId))`, then `for (const id of cfg.players) watchedPlayers.add(id)`. The change handler (line 1451) fires `void loadWatched().then(...)` per change with no serialization. Sequence: change→B clears + requests get(B); change→C clears + requests get(C); get(B) resolves and adds B's players; get(C) resolves and adds C's players → watchedPlayers = B ∪ C, rescanMap = C's. Any later star toggle calls writeWatchConfig (lines 740-747), writing `players: [...watchedPlayers]` — B's ids — into C's chrome.storage key, and the in-game scan FAB then probes cross-universe player ids.
  - **Kierunek naprawy:** Serialize via a generation token (capture selectedUniverseId / a counter before the await and bail if it changed before mutating the Set), or build a fresh Set locally and swap it in atomically after the await.

- [ ] **7.2** `src/features/dashboard/index.js:1477` **[low/state-hygiene]** - _resetDashboardForTest is incomplete relative to its documented contract: it omits half the wireDom refs (targetsContainer, all tgt*, serverMap*) and none of the Targets/watch-list/API module state is reset
  - **Problem:** The JSDoc (lines 1468-1475) says it 'Re-inits the module-level lets, drops the DOM refs cached by wireDom'. The body (lines 1490-1507) nulls statsEl…freeCountInfoEl but leaves serverMapHost, serverMapView, serverMapWindow(V), serverMapFarm(V), targetsContainer and every tgt* ref pointing at the previous test's detached DOM, and leaves watchedPlayers, rescanMap, expandedTargets, targetSort, apiIndex, apiBounds, ownMilitary, apiCache, targetCandidates, targetReports, planetCountByPlayer, players, ownProfile and repaintQueued populated — state and detached nodes leak into the next installDashboard test.
  - **Kierunek naprawy:** Extend the reset to null the remaining wireDom refs and re-initialise the Targets/watch-list/API caches (watchedPlayers.clear(), rescanMap = {}, expandedTargets.clear(), targetSort = { ...DEFAULT_TARGET_SORT }, apiIndex = null, etc.).

- [ ] **7.3** `src/features/dashboard/index.js:312` **[low/other]** - installDashboard's comment claims it is 'Safe to call multiple times' but boot() is not idempotent — a second call duplicates tab/button listeners and re-subscribes installSync/installRoutes
  - **Problem:** installDashboard (lines 312-318) runs boot() unconditionally. boot calls wireTabs (click listener per .tab-btn, line 648), wireListeners (listeners on every control, line 1332 'Called once at boot; never re-wires' — only true for a single boot), installRoutes (routes.js lines 593-597: addBtn/saveBtn listeners added per install, so one click would add two routes) and installSync (sync.js line 141: a fresh chromeStore.onChanged subscription per install). Only installAlarmClock has a `wired` guard (alarmClock.js line 106).
  - **Kierunek naprawy:** Either add a module-level installed guard that makes the second call a no-op (matching the feature-install idempotency invariant), or correct the comment to state it must be called exactly once.

- [ ] **7.4** `src/features/dashboard/routes.js:151` **[low/bug]** - writeCollectConfig and save() race on the same whole-slot key: a Send-All radio click followed quickly by 'Save routes' can silently revert the radio choice in storage
  - **Problem:** Both paths are unserialized read-modify-writes of dailyRunRoutesKeyFor(uni): writeCollectConfig (lines 151-161) reads the slot then writes `{ routes, collectTarget, ...collect, ...patch }`; save (lines 563-576) reads the slot then writes `{ routes: clean, collectTarget, collectMission, collectShips, collectResources }` with the collect fields it just read. If save's chromeStore.get resolves before writeCollectConfig's set lands, save writes the pre-patch collectMission/Ships/Resources back, losing the radio change while the UI still shows it checked (model.collectMission was updated in memory, line 607).
  - **Kierunek naprawy:** Have save() write the collect fields from `model` (which syncCollectRadios keeps current) instead of re-reading them from storage, or funnel both writers through one serialized queue.

- [ ] **7.5** `src/features/dashboard/sync.js:142` **[low/complexity]** - The Sync tab re-runs a full chromeStore.getAll() + whole-body rebuild on EVERY ':oge_' key change, including the plumbing keys its sibling editors write on each save and the per-system scan writes streamed while a game tab browses the galaxy
  - **Problem:** installSync subscribes `if (Object.keys(changes).some((k) => k.includes(':oge_'))) void render();` (lines 141-143) and render() does `await chromeStore.getAll()` (line 119) — the entire multi-megabyte store (scans + apiCache slices) — then rebuilds every card. Each dashboard save writes three matching keys in a row (e.g. scanConfig.js lines 160-164: config + Ts + syncRequestAt), triggering three consecutive full snapshots; an open game tab's continuous `<id>:oge_galaxyScans` writes do the same for as long as the dashboard stays open.
  - **Kierunek naprawy:** Debounce render (e.g. 250-500 ms trailing) and/or ignore the plumbing bases (`isPlumbingBase` from syncInventory.js is already importable) when deciding to repaint.

- [ ] **7.6** `src/features/dashboard/alarmClock.js:158` **[low/async-teardown]** - installAlarmClock discards chromeStore.onChanged's unsubscribe, so _resetAlarmClockForTest cannot unhook it — each reset+reinstall cycle in tests stacks another listener that fires refreshPreview/updateTopic per storage change
  - **Problem:** Line 158-161 registers `chromeStore.onChanged((changes) => { ... void updateTopic(); ... void refreshPreview(); })` without keeping the returned unsubscribe (lib/storage.js onChanged returns an idempotent remover, lines 230-244). _resetAlarmClockForTest (lines 1059-1066) resets `wired`, the topic state and the `el` cache but has no handle to remove the listener, so a re-install after reset leaves two (then three, …) live listeners, each doing gist/ntfy fetch work per mirror-key change.
  - **Kierunek naprawy:** Store the unsubscribe in a module-level let and call it (then null it) inside _resetAlarmClockForTest, mirroring how the timer is cleared.

- [ ] **7.7** `src/features/sendExpedition/index.js:253` **[low/bug]** - paintCapLabel captures the CURRENT label as the restore target, so overlapping transients (double tap while maxed, or runPhase2's 'No ships' → paintAllMaxed) permanently leave a stale transient label — and this feature has no ticker to self-heal.
  - **Problem:** Line 253: `const original = getLabel(btn);` then a setTimeout restores it (lines 256-259). Two taps within MAX_LABEL_MS: the second call's `original` is the transient 'All maxed!' itself, so the later timer restores 'All maxed!' with BG_IDLE and it sticks (unlike sendColony/sendLifeform, sendExpedition subscribes to no 1 Hz repaint ticker — labels only change on the next click). Same defect deterministically in runPhase2: line 325 setLabel(btn, 'No ships') immediately before paintAllMaxed(btn) (line 331), so after 2 s the button restores to 'No ships' instead of BUTTON_TEXT. paintRoutineOff (lines 279-289) shares the pattern for btn.title.
  - **Kierunek naprawy:** Track the transient state explicitly (e.g. remember the pre-transient idle label / clear the pending timer before starting a new transient, restoring to BUTTON_TEXT rather than to whatever is currently painted).

- [ ] **7.8** `src/features/sendColony/index.js:477` **[low/duplication]** - onSendClick recomputes the candidate with findNextColonizeTarget only (no pickCandidateInView), so the documented 'current-view priority — you see it, Send goes there' never actually drives a send; the view candidate only tints the rim.
  - **Problem:** Lines 474-487 duplicate captureEnv+derive's input assembly (cfg, readHomePlanet, parsePositions, getApiContext, blockingCoords) but call only findNextColonizeTarget — while derive's galaxy branch (pure.js lines 518-531) prefers pickCandidateInView, whose doc promises 'a matching free slot in THAT system takes precedence over the global best candidate ... "you see it, Send goes there"' (pure.js lines 229-235). On the galaxy page the click just navigates to bareFleetdispatchUrl (s === 'off', line 537), and once on fleetdispatch view is null, so the eventual select() always targets the global pick. The view-priority path is effectively dead behavior with a misleading contract.
  - **Kierunek naprawy:** Either reuse derive()'s ctx.candidate in onSendClick (removing the duplicated picker call and restoring view priority through e.g. a remembered view candidate), or update the pickCandidateInView/derive docs to say the view candidate is paint-only.

- [ ] **7.9** `src/features/sendColony/index.js:318` **[low/complexity]** - refresh() runs the full derive+render pipeline (worst case a whole-universe candidate scan every 1 Hz tick) before the busy/eventBoxReady early-returns discard the result.
  - **Problem:** Lines 317-318 compute `const ctx = derive(captureEnv()); const result = render(ctx);` unconditionally; `if (busy) return;` (line 323) and the eventBoxReady gate (lines 329-332) then throw the work away. derive→findNextColonizeTarget iterates up to 7 galaxies × 499 systems × targets and builds a sorted 499-element array per home-galaxy pass (pure.js lines 172-204), plus captureEnv's DOM reads and blockingCoords set build — all wasted on every ticker tick while busy or while the page is still hydrating.
  - **Kierunek naprawy:** Move the busy and eventBoxReady early-returns above the derive/render computation (the armed-fleet2 branch already runs before paintZone(result.send), so only the final paint needs `result`).

- [ ] **7.10** `src/features/sendColony/index.js:523` **[low/robustness]** - Courier failure paints ('Stale', 'Timeout', 'No ship!', 'No fuel') are clobbered by the next 1 Hz ticker tick, so error feedback is visible for under a second.
  - **Problem:** After a failed retarget/select/dispatch, busy is already false and colReady false (e.g. lines 514-523, 557-566, 443-455), so the very next refresh() from the clock ticker (line 978, REPAINT_TICK_MS = 1000) falls through to `paintZone('send', result.send)` (line 368) and repaints the idle 'Colonize' label — average ~0.5 s of error visibility. sendLifeform solved exactly this with a transientPaint/transientUntil hold (index.js lines 96-169, TRANSIENT_MS 1800) and sendExpedition holds cap labels for 2 s; sendColony has no equivalent, undermining colErrorPaint's diagnosability goal ('so a failure is diagnosable at a glance', line 408).
  - **Kierunek naprawy:** Adopt sendLifeform's transient-hold pattern (paint + deadline checked in refresh()) for the colErrorPaint / 'No fuel' paints so errors persist ~2 s before the derive label resumes.

- [ ] **7.11** `src/features/alarmClock/eventList.js:376` **[low/bug]** - Wave badge membership is matched by arrival timestamp alone over ALL fleet rows, so an unrelated leg arriving the same second as an expedition-wave leg gets wave-badged and can even steal the anchor.
  - **Problem:** The pre-pass (lines 372-382) iterates every GAME.EVENT_FLEET_ROWS row and does `snapshot.waves.find((x) => (x.returnAts || []).includes(arrivalAt))` with no data-mission-type=15 / data-return-flight=true filter, while the producer defines wave membership strictly as expedition returns (producer.js RETURN_ROW_SELECTOR, lines 86-87). A coincidental same-second arrival of e.g. a transport is stamped 'member' (losing its own FS/ad-hoc control, line 399 `continue`), and if it is the earliest present it becomes the anchor (line 380) carrying the cancel/resend control instead of the real expedition row.
  - **Kierunek naprawy:** Mirror the producer's predicate in the pre-pass: only rows with data-mission-type='15' and data-return-flight='true' may map into waveOf/anchor.

- [ ] **7.12** `src/features/manualFsMark/index.js:214` **[low/async-teardown]** - manualFsMark's 150 ms debounced sync is not guarded by `installed`, so a mutation just before dispose resurrects the chip after teardown — and dispose never removes the injected style element.
  - **Problem:** Line 214-216: `const debounced = debounce(sync, 150); observer = new MutationObserver(debounced); observer.observe(document.body, ...)`. lib/debounce has no cancel by design, and `sync()` (lines 172-199) has no installed guard: if the debounce is pending when dispose (lines 218-223) disconnects the observer and removes the chip, the late fire re-runs sync, finds no `#CHIP_ID`, finds an anchor and inserts a brand-new chip with a live click handler that nothing will ever remove. Sibling features guard exactly this: traderMenuHighlight.js:970 `debounce(() => { if (installed) refresh(); })`, badges/index.js:775-777, and eventList.js:524. Dispose also leaves the injected `<style id="oge-mfs-style">` (line 213) in the DOM, unlike eventList.js:634 / badges / threatHighlight which remove theirs.
  - **Kierunek naprawy:** Wrap the debounced callback as `debounce(() => { if (installed) sync(); }, 150)` and remove the STYLE_ID style node in dispose.

- [ ] **7.13** `src/features/abandon/colonyFab.js:304` **[low/bug]** - In the 'navigate' state the mounted button keeps a stale `fresh` closure: refresh() compares only the state id, so a change of WHICH colony is the first fresh-small candidate never remounts the button
  - **Problem:** refresh() early-returns when `want === currentState` (line 304). mountFor('navigate') captures `fresh` (cp, coords, max) at mount time (lines 252-267) into the onTap closure and the label. If the flagged colony stops qualifying (a field gets built, or it's abandoned from another device) while a DIFFERENT fresh-small colony exists, desiredState() still returns 'navigate', so the button keeps the old target: wrong '`N` fields' label and a tap navigates to the no-longer-relevant colony's overview (it self-corrects only after that wrong navigation).
  - **Kierunek naprawy:** Track the mounted target's cp alongside currentState and remount when `fresh.cp` differs, not only when the state id changes.

- [ ] **7.14** `src/features/abandon/colonyFab.js:334` **[low/state-hygiene]** - dispose() ends a running flow via endFlow() without calling flow.cancel(), leaking the module-level flowActive guard in abandon/index.js so no abandon flow can ever be created again
  - **Problem:** endFlow() (lines 205-209) sets `flow = null` and removes the guard but never invokes the flow's `cancel()` — the only thing that releases `flowActive` in src/features/abandon/index.js (line 139/282) short of the flow reaching done/aborted. dispose() calls endFlow() (line 334), so disposing the FAB mid-flow (or a test/dispose cycle that doesn't also call _resetAbandonForTest) leaves flowActive=true and createAbandonFlow() returns null until a full page reload.
  - **Kierunek naprawy:** In endFlow(), call `flow?.cancel()` before nulling the reference so the re-entry guard in abandon/index.js is always released.

- [ ] **7.15** `src/features/abandon/colonyFab.js:329` **[low/complexity]** - Body-wide MutationObserver invokes refresh() with no debounce, re-running the full planet-list parse (regex over every row tooltip) on every mutation batch, including OGame's 1 Hz event-ticker countdown churn
  - **Problem:** `new MutationObserver(refresh)` observing document.body with `{ childList: true, subtree: true }` (lines 329-330). refresh() → desiredState() → checkAbandonState() (URL check + tooltip regex) + findFirstFreshPlanet() which parsePlanetRow's every `.smallplanet` (two regexes per row) — executed per mutation batch. The sibling badges feature debounces the same class of observer at 200 ms (badges/index.js:775-778); this one runs raw.
  - **Kierunek naprawy:** Wrap refresh in the shared debounce (as badges does) — the FAB state changing 200 ms late is imperceptible.

- [ ] **7.16** `src/features/abandon/colonyFab.js:241` **[low/async-teardown]** - colonyFab schedules `setTimeout(refresh, 100)` after an aborted flow without an `installed` guard, so a dispose inside that window remounts the FAB module post-teardown.
  - **Problem:** Line 241 (`setTimeout(refresh, 100)` in onAbandonTap's 'aborted' branch) is untracked and `refresh()` (lines 299-306) checks only `flowRunning` and `fabMode` — not `installed`. Dispose (lines 333-340) runs endFlow+unmount and sets installed=null, but a pending timeout then calls refresh → desiredState() → mountFor(), which calls makeButton/registerFabModule and re-creates the button with no owner left to dispose it. The feature's own DOMContentLoaded fallback at line 310 does guard with `if (installed) refresh()`, so this is an inconsistent omission.
  - **Kierunek naprawy:** Guard the timeout callback (`setTimeout(() => { if (installed) refresh(); }, 100)`) or add `if (!installed) return;` at the top of refresh(), matching the line-310 convention.

- [ ] **7.17** `src/features/settingsUi/controls.js:387` **[low/bug]** - buildInputControl reverts invalid numeric input to the stale build-time value captured in a closure, silently overwriting later valid edits, and leaves the garbage text displayed
  - **Problem:** Line 369 captures `const currentValue = readSetting(opt.id)` at row-build time; the change handler at line 387 does `nextValue = Number.isFinite(n) ? n : currentValue` — so if the user first sets a valid number (store updated) and then types a non-numeric string into the same still-mounted row, writeSetting writes the OLD build-time value back into the store, not the current one. Additionally the handler never resets input.value on the invalid branch, and the subscriber-driven resync is suppressed by writingFromUi (line 833), so the input keeps displaying the garbage string while the store holds a number. buildDurationControl (lines 553-561) handles the same case correctly by re-rendering from the store. Currently latent: no live section uses type 'text'/'password' (see the dead-code finding).
  - **Kierunek naprawy:** On the invalid branch read the CURRENT store value (readSetting(opt.id)) instead of the captured currentValue, and reset input.value from it, mirroring buildDurationControl.

- [ ] **7.18** `src/features/settingsUi/controls.js:197` **[low/state-hygiene]** - refreshAsyncStatus's module-scope lastKey survives an AGR panel rebuild, so a rebuilt asyncStatus row is never painted and stays blank
  - **Problem:** asyncStatusState is a module-scope Map (line 148) that persists across panel rebuilds. When AGR rebuilds #ago_menu_content, settingsUi/index.js re-injects a brand-new tab: buildAsyncStatusControl's initial probe bails because the new span is detached (line 193-194, deliberately without recording lastKey), but the post-append syncInputsFromState -> refreshAsyncStatus(opt) then hits `if (!force && key === st.lastKey) return;` (line 197) — lastKey still holds the value recorded during the FIRST build — so the freshly created empty span is never given text until the refreshKey changes, a refreshEvent fires, or the manual button is clicked. The in-code comment (lines 186-192) documents exactly this blank-row failure mode for the initial build but the fix doesn't cover rebuilds. Currently latent: no live section uses asyncStatus.
  - **Kierunek naprawy:** Treat an empty/`Checking…`-less span as unpainted (e.g. also probe when span.textContent is empty), or clear asyncStatusState entries when the tab is rebuilt/disposed.

- [ ] **7.19** `src/features/shared/draggableButton.js:230` **[low/robustness]** - installDrag handles touchend but not touchcancel, so a cancelled touch drag never persists the position and leaves isDragging stale.
  - **Problem:** Only `element.addEventListener('touchend', () => { onEnd(); })` is wired (line 230); a touchcancel (incoming call, browser gesture, tab switch mid-drag) skips onEnd, so the moved position styled at lines 195-196 is never written to safeLS (posKey) — on the next page load the button snaps back — and `isDragging` stays true until the next start.
  - **Kierunek naprawy:** Add `element.addEventListener('touchcancel', onEnd)` alongside touchend.

- [ ] **7.20** `src/sync/scheduler.js:1003` **[low/state-hygiene]** - The cloudSync-off install path caches a noop dispose that never clears `installed`, so the scheduler can never be (re)installed in the same JS context after the user enables cloud sync
  - **Problem:** `if (!settingsStore.get().cloudSync) { const noop = () => {}; installed = { dispose: noop }; return noop; }` — the noop does not set `installed = null`, while the JSDoc contract (lines 991-992) says the dispose fn 'clears the install handle' and the header comment (lines 986-989) says the bootstrap 'is expected to re-install the scheduler in response to that settings change'. Any subsequent installSync() hits `if (installed) return installed.dispose;` (line 995) and returns the stale noop forever; even calling the returned dispose first doesn't help. Only OGame's constant page reloads (fresh module state) mask this in production.
  - **Kierunek naprawy:** Make the opt-out dispose clear the handle: `const noop = () => { installed = null; }; installed = { dispose: noop };` so a re-install after flipping cloudSync on actually installs.

- [ ] **7.21** `src/sync/ntfyReconciler.js:534` **[low/robustness]** - reconcileQueue never sweeps duplicate queued messages sharing a wanted fire time, so a concurrent-producer race (two game tabs) leaves permanent double notifications
  - **Problem:** `queuedByTime` keeps only the FIRST id per fire time (line 503, 'first wins'), and the orphan sweep is `ours.filter((m) => !wantedTimes.has(m.time))` — a second message queued at the same wanted time is neither reused nor cancelled. Two tabs of the same universe both run syncAlarmClock; each polls before the other's POST lands, both post the same slot, and every later reconcile pass keeps both (the time is wanted), so the user gets each alarm twice until the slot fires.
  - **Kierunek naprawy:** Extend the sweep to also cancel `ours` entries whose time IS wanted but whose id differs from the one chosen in queuedByTime — the reconcile pass already has all the data to do it.

- [ ] **7.22** `src/sync/scheduler.js:956` **[low/async-teardown]** - dispose() does not neutralise a pending debouncedUpload timer, so a full gist round-trip can still fire up to 15 s after the scheduler is torn down
  - **Problem:** `debouncedUpload` is a module-scope `debounce(() => { void upload(); }, DEBOUNCE_MS)`; lib/debounce.js deliberately exposes no cancel(), and neither the dispose fn (lines 1228-1239) nor _resetSchedulerForTest clears the pending timer. A scheduleUpload() shortly before dispose leaves a live 15 s timer that runs upload() — a network fetch + potential PATCH — after every subscription/listener was removed (canStartSync only gates on cloudSync/token/inFlight, not on installed).
  - **Kierunek naprawy:** Gate the debounced body on the install handle (`if (!installed) return;`) or track a generation token bumped in dispose, so a post-dispose timer becomes a no-op without adding cancel() to the minimal debounce.

- [ ] **7.23** `src/domain/waves.js:271` **[low/bug]** - A mid-wave expedition recall can split one live wave's cluster in two; the greedy overlap match then re-stamps the tail half as a brand-new wave with a fresh reminder schedule — the exact double-schedule failure the returnAts identity was designed to prevent.
  - **Problem:** clusterWaves (line 177) groups returns by consecutive gaps <= 300 s, and reconcileWaves matches each candidate greedily: findBestOverlap (lines 207-222) skips prev waves already in `consumed`, so when a stored wave with returnAts [1000, 1250, 1500] loses its middle member (a recalled expedition's data-arrival-time moves earlier), the remaining [1000, 1500] split into two candidates that BOTH overlap the same prev wave; the first consumes it, the second falls into the `!match` branch (line 283-294), gets a new id 'w_1500' plus detectedAt=now, and sets brandNewDetected=true — which can additionally tombstone the matched half via the cleanup rule at lines 315-323 if its nextWaveAt is within 60 s.

- [ ] **7.24** `src/domain/histogram.js:352` **[low/robustness]** - collectGalaxyStats' galaxy-key parsing contradicts its documented defensive behaviour: a key with a leading colon is tallied under galaxy 0 and a colon-less numeric key is accepted, instead of being skipped as malformed.
  - **Problem:** Line 352: `const g = Number(key.split(':')[0]); if (!Number.isFinite(g)) continue;` — for key ':5', split gives '' and Number('') is 0 (finite), creating a byGalaxy[0] bucket; for key '7' (no colon at all), g=7 is counted. The JSDoc at lines 336-337 claims "a malformed system key (missing ':', non-numeric galaxy) is silently skipped", and the sibling parser in regions.js collectMatchesAndMines (lines 783-787) uses the stricter `colonIdx <= 0` guard for the same key format.
  - **Kierunek naprawy:** Mirror the regions.js guard: reject keys whose first ':' index is <= 0 before parsing the galaxy number, so the code matches the documented contract and the two scan-map parsers can't diverge.

- [ ] **7.25** `src/state/sharedSettings.js:98` **[low/state-hygiene]** - initSharedSettings' idempotency guard is checked before its awaits (set only after two awaits), so overlapping calls double-register the onChanged listener and orphan one
  - **Problem:** Line 98 `if (unsub) return;` runs before `await chromeStore.get(...)` (line 99) and a conditional `await chromeStore.set(...)` (line 104), but `unsub` is only assigned at line 108 after both awaits settle. Two calls that overlap in that async window both pass the guard, both run the merge + conditional chrome.storage write, and both register a chromeStore.onChanged listener; the second assignment to `unsub` overwrites the first, so disposeSharedSettings (line 120-125) can only ever remove one listener — the other survives teardown and keeps writing into settingsStore. The JSDoc explicitly promises 'Idempotent — a second call before disposeSharedSettings is a no-op', which only holds for calls made after the first one fully resolves. Every other init* in state/ establishes its guard synchronously (e.g. state/scans.js:205-214); content.js:112 fire-and-forgets this one (`void initSharedSettings()`), so nothing serializes callers.
  - **Kierunek naprawy:** Make the guard synchronous: store the in-flight promise (`let initPromise = null; if (initPromise) return initPromise; initPromise = (async () => {...})();`) or set a boolean sentinel before the first await.

- [ ] **7.26** `src/lib/persist.js:96` **[low/robustness]** - The async hydrate branch has no rejection handling, so a rejecting load() leaves onHydrate unfired forever — permanently stalling every when*Hydrated() consumer with an unhandled rejection
  - **Problem:** Line 96-99: `loaded.then((v) => { if (...) store.set(v); onHydrate?.(); })` has no `.catch`. If a load callback throws (state loads run normalizers over persisted blobs inside the async fn, e.g. state/scans.js:222 stripToLfMarkers, state/bodies.js:177 narrowInventory), the promise rejects unhandled and onHydrate never runs — so hydratedPromise in state/history.js:235, state/bodies.js:174, state/targets.js:93 and state/dailyRunRoutes.js:211 never resolves. Consumers that await it before every write (state/targets.js:74 recordReport, features/colonyRecorder.js:261, features/planetBarCapture.js:239) then hang silently for the page lifetime — the feature is dead with no log.
  - **Kierunek naprawy:** Wrap the async branch in .then(...).catch(err => { logger.error(...); onHydrate?.(); }) so hydration settles (with the store's initial value) even when load fails.

## Etap 8: Dokumentacja i komentarze

Komentarze klamiace o kodzie + drift w CLAUDE.md (lista sankcjonowanych plain-modulow w state/). NIE dotykac komentarzy game-knowledge.

Sugerowany commit: `docs: fix comments contradicting code + CLAUDE.md drift`

- [ ] **8.1** `src/features/dailyRun/index.js:102` **[low/other]** - Header comment documents the collect-zone long-press as '300ms+' but LONG_PRESS_MS is 2000 — the doc misstates the actual interaction by ~7x
  - **Problem:** Line 18-19 in the module header: 'LONG-PRESS (300ms+) marks the body you're standing on as that collect target', while line 102 defines `const LONG_PRESS_MS = 2000;` which is passed as `holdMs` to the shared button (line 758) — the real hold threshold is 2 s, so anyone tuning or debugging the gesture from the header doc is misled.
  - **Kierunek naprawy:** Update the header comment to '2s+' (or reference LONG_PRESS_MS instead of a literal).

- [ ] **8.2** `src/features/traderMenuHighlight.js:977` **[low/other]** - Stale comments contradict the code: the observer comment claims a '150 ms debounce' while REFRESH_DEBOUNCE_MS is 300, and the lifecycle header says dispose 'clears the poll' though the poll was replaced by clock.scheduleAt
  - **Problem:** Line 977 ('the 150 ms debounce coalesces bursts') vs line 406 (`const REFRESH_DEBOUNCE_MS = 300`); header line 86 ('Dispose strips classes, removes the style, disconnects the observer, clears the poll, ...') vs the actual dispose (lines 997-1006), which disposes the scheduleAt boundary handle — there is no poll anywhere in the module (install uses clock.scheduleAt at line 963, and refresh's doc at line 910 also still mentions 'the safety-poll').
  - **Kierunek naprawy:** Update the three comment sites to match the current design (300 ms debounce; boundary timer via clock.scheduleAt, no poll).

- [ ] **8.3** `src/sync/alarmClock.js:9` **[low/other]** - Header comment says the sync payload is 'schema 3' and that fetchGistData rejects anything that isn't schema 3 — the actual pinned SCHEMA_VERSION is 1
  - **Problem:** alarmClock.js lines 8-10: 'The cross-device sync payload lives in `oge-data.json.gz.b64` (gzip+base64, schema 3) and `fetchGistData` rejects anything that isn't schema 3.' gist.js:252 declares `const SCHEMA_VERSION = 1;` and fetchGistData (gist.js:661) rejects `parsed.version !== SCHEMA_VERSION`. The '3' appears to be confusion with the alarm file's own v3 filename migration described further down the same header.
  - **Kierunek naprawy:** Correct the two 'schema 3' mentions to 'schema 1' (or drop the number and reference gist.js's SCHEMA_VERSION) so the next reader doesn't chase a phantom version bump. This is internal doc rot, not protected game knowledge.

- [ ] **8.4** `src/bridges/checkTargetObserver.js:114` **[low/other]** - installCheckTargetObserver's docstring contradicts the code: it claims the handler bails on status !== 'success', but dispatching on failures is the deliberate contract
  - **Problem:** The docstring (lines 112-114) says the observer '2. Parses the JSON response; bails silently on invalid JSON or on `status !== \'success\'`', while the handler body explicitly does the opposite — the NOTE at lines 143-147 states 'we deliberately do NOT filter on `status === \'success\'` ... Dispatching on BOTH success and failure responses is the contract', and no such gate exists. The leftover `const success = parsed.status === 'success'` is now unused except for the `void success;` at line 179, whose own comment ('kept in scope for the `if (!success) return` gate above (actually there's no gate ...)') admits it is vestigial from a removed gate.
  - **Kierunek naprawy:** Fix the docstring bullet to state that both success and failure responses dispatch (failures carrying errorCode), and delete the dead `success` variable plus the `void success;` line.

- [ ] **8.5** `src/content.js:99` **[low/other]** - Bootstrap comment 'All stores hydrate synchronously' is false — most stores hydrate asynchronously from chrome.storage, the exact race the codebase guards against elsewhere
  - **Problem:** Lines 99-102 claim 'All stores hydrate synchronously. localStorage is per-origin and the chrome.storage stores are namespaced per-universe...'. Only settings/registry (localStorage-backed) hydrate synchronously; history, scans, players, dailyRunRoutes, galaxyScanConfig, alarmClockConfig, bodies, colonizeDecisions, targets, and watchList all load via `chromeStore.get` Promises (e.g. state/history.js:124-127 documents 'hydration is async ... until that tick, consumers see []', and whenHistoryHydrated exists precisely because a feature racing the async hydrate lost data on Firefox). A reader trusting this comment could reintroduce the hydrate-clobber race the when*Hydrated gates were built to fix.
  - **Kierunek naprawy:** Correct the comment: localStorage-backed stores (settings, registry) hydrate synchronously; the chrome.storage-backed stores hydrate asynchronously and expose when*Hydrated gates that consumers must await before their first write.

- [ ] **8.6** `src/state/fleetSaveSet.js:10` **[low/other]** - The sanctioned plain key-owner list in the architecture contract has drifted: state/ now has seven store-less key-owner modules, not the three CLAUDE.md enumerates, and its createStore/dispose count claim is stale
  - **Problem:** state/fleetSaveSet.js:10 and state/manualLandedFs.js:24 self-describe as 'Plain key-owner over safeLS (NO reactive store)', and state/badgeCache.js:25 and state/apiCache.js:20 are likewise plain read/write owners — none of them is in CLAUDE.md's sanctioned-exception list (dailyActions, lifeformArtifacts, ownProfile only). CLAUDE.md's parenthetical 'state/ has one more createStore than dispose*Store export — these three own no store' is also arithmetically stale: state/ currently has 12 createStore calls and 12 dispose*Store exports (plus the extra disposeScansListener/disposePlayersListener teardowns). The code pattern itself is fine and consistent; the contract text no longer matches it, so a future audit or session following CLAUDE.md literally would flag correct code (or trust a stale count).
  - **Kierunek naprawy:** Update the CLAUDE.md sanctioned-exception paragraph to enumerate all current plain key-owner modules (or restate the rule structurally: 'store-less modules are sanctioned when nothing subscribes; each documents this in its header') and drop the stale count parenthetical.

## Odlozone (wymagaja osobnej rozmowy projektowej - NIE robic mimochodem)

- `src/features/traderMenuHighlight.js:454` - 948-line single-file feature mixes its pure policy core (traderGlows, nextTraderBoundary) with DOM painting, observers, and chips UI — past the ~400-line mixed threshold without a pure.js
- `src/features/dashboard/alarmClock.js:269` - 983-line dashboard AlarmClock tab keeps exported pure helpers (collectOurMessageIds, isFleetSaveTooFarOut, guardianMessagesFor) inline amid gist fetch + DOM rendering

## Odrzucone przez weryfikatorow (bez akcji - dla kompletnosci)

- ~~src/features/alarmClock/eventList.js:321 effectiveFs dereferences fs.offsetsSec[i] unguarded on the line after explicitly guarding fs.offsetsSec with `|| []`, so a mirror entry missing offsetsSec but carrying fireAts throws during render.~~
- ~~src/features/shared/fabSettingsLifecycle.js:80 A fabMode ON-flip that arrives before document.body exists is silently dropped and never retried, so the button stays unmounted until another OFF/ON toggle.~~
- ~~src/features/shared/fleetCourier.js:444 The 500 ms checkTarget fallback race also fires on genuine first-pass flows with a slow response, silently skipping noMoon/reserved/mission validation.~~
- ~~src/features/shared/unifiedFab.js:227 Satellite orbs freeze at the wrap z-index captured at build time, so after the FAB is raised by bringToFront other floating buttons can paint over the orbs.~~
- ~~src/domain/players.js:207 occupantStrength checks the buddy/allianceMember exclusion AFTER the newbie/strong/honorable band flags, so a friend or own-alliance member carrying any of those flags is labelled a target band instead of being excluded to null as the doc contract states.~~
- ~~src/lib/persist.js:96 An async load() rejection in persist() is unhandled and permanently hangs the store's hydration gate (onHydrate never fires)~~

