# Build artefacts
/dist/
/release/
*.zip

# Wygenerowana strona dokumentacji. NIE commitujemy jej: na GitHub Pages
# publikuje ją .github/workflows/pages.yml (build z treści + artefakt), więc
# opublikowany HTML jest zawsze świeży, a w repo nie ma pliku do skonfliktowania.
site/dist/

# Test coverage report (npm run coverage)
coverage/

# Claude Code per-project state
.claude/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*

# Type-check cache
*.tsbuildinfo

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db
.DS_Store

# Logs & temp
*.log
*.tmp

# Secrets — code never stores tokens in files, but belt + suspenders
.env
.env.local
.env.*.local
*.token
*.key
secrets.json
