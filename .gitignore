# Build artifacts
dist/
*.zip

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db
.DS_Store

# Node (jeśli kiedyś w v5 dojdzie npm)
node_modules/
npm-debug.log*
yarn-debug.log*

# v5.0.0 build (gdy ruszy)
v5.0.0/dist/
v5.0.0/node_modules/

# Logs & temp
*.log
*.tmp

# Type-check cache (v5 tsc --noEmit)
*.tsbuildinfo

# Secrets — belt + suspenders; kod NIE trzyma tokenów w plikach,
# ale ignorujemy obvious names gdyby kiedyś coś się dostało na dysk
.env
.env.local
.env.*.local
*.token
*.key
secrets.json
