{
  "name": "og-e",
  "version": "1.0.2",
  "description": "UI helper for OGame: floating Send Exp/Col, galaxy scan tracker, abandon overlay. Requires AntiGameReborn. See README.md.",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && rollup -c && node scripts/copy-static.mjs",
    "build:prod": "npm run clean && cross-env NODE_ENV=production rollup -c && node scripts/copy-static.mjs",
    "dev": "rollup -c -w",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "npm run build:prod && node scripts/package.mjs",
    "package:source": "node scripts/package-source.mjs"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^5.0.7",
    "@rollup/plugin-terser": "^1.0.0",
    "cross-env": "^10.1.0",
    "happy-dom": "^14.12.0",
    "rollup": "^4.18.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
