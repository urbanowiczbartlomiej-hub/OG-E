{
  "name": "og-e-v5",
  "version": "5.0.0-dev",
  "description": "OG-E v5 — clean-slate rewrite of the OGame UI helper. See DESIGN.md.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rollup -c && node scripts/copy-static.mjs",
    "dev": "rollup -c -w",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^5.0.7",
    "happy-dom": "^14.12.0",
    "rollup": "^4.18.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
